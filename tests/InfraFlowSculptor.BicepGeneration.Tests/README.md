# InfraFlowSculptor.BicepGeneration.Tests

Unit tests for the staged Bicep generation pipeline (Vague 1).

> Scaffold project. No test classes yet — to be filled in the next phase.

## Scope

Each `Pipeline/Stages/*Stage.cs` and each `TextManipulation/*.cs` helper exposed by
`InfraFlowSculptor.BicepGeneration` is independently unit-testable from this assembly.

## Conventions

See `.github/skills/xunit-unit-testing/SKILL.md`. Naming: `Given_X_When_Y_Then_Z`.
