# Release Application Pipeline for myfunc — Auto-generated by InfraFlowSculptor
# Expected CI pipeline definition name: core - myfunc - CI
name: $(Date:yyyyMMdd).$(Rev:r)

trigger: none

resources:
  pipelines:
    - pipeline: ci
      source: 'core - myfunc - CI'
      trigger:
        branches:
          include:
            - main

extends:
  template: ../../../Common/pipelines/app-release-code.pipeline.yml
  parameters:
    resourceName: 'myfunc'
    resourceType: 'FunctionApp'
    environments:
      - shortName: 'dev'
        name: 'dev'
        variablesPath: '../../variables/dev.yml'
        variableGroups: []
