# Release Application Pipeline for myweb — Auto-generated by InfraFlowSculptor
# Expected CI pipeline definition name: core - myweb - CI
name: $(Date:yyyyMMdd).$(Rev:r)

trigger: none

resources:
  pipelines:
    - pipeline: ci
      source: 'core - myweb - CI'
      trigger:
        branches:
          include:
            - main

extends:
  template: ../../../Common/pipelines/app-release-container.pipeline.yml
  parameters:
    resourceName: 'myweb'
    resourceType: 'WebApp'
    imageRepository: 'myweb'
    promotionStrategy: 'AcrImport'
    environments:
      - shortName: 'dev'
        name: 'dev'
        variablesPath: '../../variables/dev.yml'
        variableGroups: []
