# Run linting command — Auto-generated by InfraFlowSculptor

parameters:
  - name: lintCommand
    type: string

steps:
  - powershell: |
      $command = '${{ parameters.lintCommand }}'
      Write-Host "Run linting: $command"
      Invoke-Expression $command
      if ($LASTEXITCODE -ne 0) {
          throw "Linting failed with exit code $LASTEXITCODE"
      }
    displayName: 'Run linting'