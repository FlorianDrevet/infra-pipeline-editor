# PR validation job for code-mode applications - Auto-generated by InfraFlowSculptor

parameters:
  - name: resourceName
    type: string
  - name: resourceType
    type: string
  - name: runtimeStack
    type: string
    default: 'DOTNETCORE'
  - name: runtimeVersion
    type: string
    default: '8.0'
  - name: sourcePath
    type: string
    default: '.'
  - name: testCommand
    type: string
    default: ''
  - name: buildCommand
    type: string
    default: ''

jobs:
  - job: ValidateApplicationCode
    displayName: 'Validate pull request application code'
    steps:
      - checkout: self
        fetchDepth: 0

      - template: ../steps/app-sdk-setup.step.yml
        parameters:
          runtimeStack: ${{ parameters.runtimeStack }}
          runtimeVersion: ${{ parameters.runtimeVersion }}

      - template: ../steps/app-build-code.step.yml
        parameters:
          runtimeStack: ${{ parameters.runtimeStack }}
          sourcePath: ${{ parameters.sourcePath }}
          testCommand: ${{ parameters.testCommand }}
          buildCommand: ${{ parameters.buildCommand }}