# Release Application Pipeline for myapi — Auto-generated by InfraFlowSculptor
# Expected CI pipeline definition name: core - myapi - CI
name: $(Date:yyyyMMdd).$(Rev:r)

trigger: none

resources:
  pipelines:
    - pipeline: ci
      source: 'core - myapi - CI'
      trigger:
        branches:
          include:
            - main

extends:
  template: ../../../Common/pipelines/app-release-container.pipeline.yml
  parameters:
    resourceName: 'myapi'
    resourceType: 'ContainerApp'
    imageRepository: 'myapi'
    promotionStrategy: 'AcrImport'
    environments:
      - shortName: 'dev'
        name: 'dev'
        variablesPath: '../../variables/dev.yml'
        variableGroups: []
