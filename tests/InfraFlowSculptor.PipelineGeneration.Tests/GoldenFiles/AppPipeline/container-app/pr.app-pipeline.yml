# PR Application Pipeline for myapi - Auto-generated by InfraFlowSculptor
# Expected PR pipeline definition name: [Code] core - myapi - PR
name: $(Date:yyyyMMdd).$(Rev:r)

trigger:
  branches:
    include:
      - '*'
  paths:
    include:
      - core/myapi/*
      - .azuredevops/Common/*
      - .azuredevops/core/apps/MyApi/*

pr:
  branches:
    include:
      - main
      - develop
      - release/*
  paths:
    include:
      - core/myapi/*
      - .azuredevops/Common/*
      - .azuredevops/core/apps/MyApi/*

extends:
  template: ../../../Common/pipelines/app-pr-container.pipeline.yml
  parameters:
    resourceName: 'myapi'
    configName: 'core'
    resourceType: 'ContainerApp'
    imageRepository: 'myapi'
    imageTagPattern: '{buildNumber}-{shortSha}'
    dockerfilePath: 'src/Api/Dockerfile'
    buildContext: '.'
