# Shared Checkout Step Template — Auto-generated by InfraFlowSculptor

parameters:
  - name: directories
    type: object
    default: []

steps:

  - ${{ if gt(length(parameters.directories), 0) }}:
    - checkout: self
      sparseCheckoutDirectories: ${{ join(' ', parameters.directories) }}

  - ${{ if eq(length(parameters.directories), 0) }}:
    - checkout: self
      clean: true
