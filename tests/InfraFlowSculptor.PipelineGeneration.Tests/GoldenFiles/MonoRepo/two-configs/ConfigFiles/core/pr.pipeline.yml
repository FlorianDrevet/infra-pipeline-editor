# PR Validation Pipeline — Auto-generated by InfraFlowSculptor
name: $(date:yyyyMMdd)

pr:
  branches:
    include:
      - main
      - develop
      - release/*
  paths:
    include:
      - core/*
      - .azuredevops/Common/*
      - .azuredevops/core/*

pool:
  vmImage: 'ubuntu-latest'

extends:
  template: ../Common/pipelines/pr.pipeline.yml
  parameters:
    projectName: core
