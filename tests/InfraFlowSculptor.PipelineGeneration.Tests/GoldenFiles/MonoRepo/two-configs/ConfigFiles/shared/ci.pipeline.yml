# CI Pipeline — Auto-generated by InfraFlowSculptor
name: $(date:yyyyMMdd)

trigger:
  branches:
    include:
      - release/*
  paths:
    include:
      - shared/*
      - .azuredevops/Common/*
      - .azuredevops/shared/*

pool:
  vmImage: 'ubuntu-latest'

extends:
  template: ../Common/pipelines/ci.pipeline.yml
  parameters:
    continueOnError: true
    projectName: shared
