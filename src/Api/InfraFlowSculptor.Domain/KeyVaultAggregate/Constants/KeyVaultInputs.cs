namespace InfraFlowSculptor.Domain.KeyVaultAggregate.Constants;

/// <summary>Available input types for a Key Vault resource (e.g. secret references).</summary>
public enum KeyVaultInputs
{
    SecretReference = 1,
}