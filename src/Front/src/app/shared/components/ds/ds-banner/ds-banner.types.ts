/**
 * Visual variant of {@link DsBannerComponent}. Drives the leading indicator
 * bar color, icon color, and accessible role mapping.
 */
export type DsBannerVariant = 'info' | 'success' | 'warning' | 'danger';
