/**
 * Tone variants accepted by {@link DsCardMatComponent}.
 *
 * `neutral` renders the default subtle border. The other tones add a 2px top
 * accent stripe coloured with the matching design system semantic token.
 */
export type DsCardMatTone = 'neutral' | 'brand' | 'success' | 'warning' | 'danger';
