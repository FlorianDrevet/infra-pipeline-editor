/**
 * Available sizes for {@link DsSpinnerComponent}.
 *
 * Mapped to a fixed pixel diameter:
 * - `sm` → 14px
 * - `md` → 18px
 * - `lg` → 24px
 * - `xl` → 40px
 */
export type DsSpinnerSize = 'sm' | 'md' | 'lg' | 'xl';
