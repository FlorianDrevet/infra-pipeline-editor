/**
 * Visual variant of {@link DsSkeletonComponent}.
 *
 * - `box`: rectangular block (radius `--ifs-radius-sm`).
 * - `line`: thin pill (radius `--ifs-radius-pill`).
 * - `circle`: circular avatar/icon placeholder.
 */
export type DsSkeletonVariant = 'box' | 'line' | 'circle';
