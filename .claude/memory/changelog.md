# Changelog
- [2026-08-31] architect (plan) + dotnet-dev + main-thread — D09 fix (bootstrap inatteignable en MultiRepo), lots 1-4 (backend). Agent `architect` a diagnostiqué l'asymétrie racine : contrairement à Bicep (F11b) et Pipeline (F12), le bootstrap n'avait qu'un niveau (projet), qui échoue toujours en MultiRepo (pas de dépôt projet, chaque config a les siens). Lot 1 : `GenerateProjectBootstrapPipelineCommandHandler`/`PushProjectBootstrapPipelineToGitCommandHandler` gardés explicitement contre MultiRepo via `Project.CanGenerateAllFromProjectLevel()` (même pattern que `GenerateProjectBicepCommandHandler`/`GenerateProjectPipelineCommandHandler`), au lieu d'échouer silencieusement dans le resolver. Lots 2-3 : nouveau slice CQRS niveau config `InfrastructureConfig/Commands/{GenerateBootstrap,PushBootstrapToGit,DownloadBootstrap}` + `Queries/GetBootstrapFileContent`, mirroring exact de `GenerateBicep`/`GeneratePipeline`, réutilisant `IProjectBootstrapDefinitionBuilder.BuildAsync` **sans le modifier** (liste `configs: [configReadModel]` au lieu de toutes les configs du projet), `Mode = BootstrapMode.FullOwner` (jamais `ApplicationOnly`, réservé à la topologie fixe SplitInfraCode — pas de notion produit "config propriétaire du bootstrap" introduite, conforme à la préférence du porteur). Lot 4 : Contracts (`GenerateBootstrapRequest/Response`, `PushBootstrapToGitRequest/Response`), `BootstrapGenerationController` (4 routes sous `/generate-bootstrap`), `Routes.GenerateBootstrap`. Interrompu par une limite de session juste avant la fin du lot 4 (Contracts/Controller/Routes déjà créés) ; terminé par le thread principal : `app.UseBootstrapGenerationController();` manquait dans `Program.cs` (seul bloqueur), aucun ajout Mapster nécessaire (mapping par convention, comme `PushPipelineToGitResponse`). Validation : `dotnet build` 0 erreur, `dotnet test` 51 échecs = baseline exacte du 2026-08-28, aucune régression. Défaut adjacent découvert par le front pendant le lot 6 (D12, non corrigé) : le bouton Push to Git unique de l'en-tête `config-detail` ne pousse jamais le Pipeline. Tracker complet : `docs/features/multirepo-bootstrap-d09-implementation-tracker.md`. `docs/stabilization/NEXT.md` et `feature-map.md` mis à jour (D09 fermé back+front, statut reste 🔴/🟡 tant que non revalidé en conditions réelles — différé jusqu'à D10, chantier suivant).
- [2026-08-31] angular-front — D09 lots 5-6 (frontend): added config-level Bootstrap generation to `config-detail`, mirroring the existing Bicep/Pipeline pattern (F11b/F12). Lot 5: `shared/interfaces/bootstrap-generator.interface.ts` + `shared/services/bootstrap-generator.service.ts` (`basePath = '/generate-bootstrap'`), exact mirror of `pipeline-generator.*`. Lot 6: 3rd tab `TAB_BOOTSTRAP` (icon `rocket_launch`) in `config-detail-generation-section.component.ts/.html`, bootstrap signals/actions in the controller (`generateAll` now runs bicep+pipeline+bootstrap together), `buildConfigBootstrapNodes` flat-file helper in `config-detail-tree.helpers.ts`, dedicated "Push to Git" button on the tab wired to a new private `config-detail.component.ts#openBootstrapPushToGitDialog()` (posts `isBootstrap: true`, deliberately separate from the ambiguous header push button — tracked defect D12, not fixed here), and `push-to-git-dialog.component.ts` now routes `isBootstrap` to `BootstrapGeneratorService`. i18n: `CONFIG_DETAIL.GENERATION.TAB_BOOTSTRAP` + full `CONFIG_DETAIL.BOOTSTRAP.*` group in `en.json`/`fr.json`. Fixed two pre-existing broken DOM-query patterns found while adding tests (`config-detail-generation-section.component.spec.ts`'s `getButton` was clicking the `app-ds-button` host instead of the inner `<button>`; `push-to-git-dialog.component.spec.ts` was querying the first `DsButtonComponent` match — "Close" — instead of "Push"). Validation: `npm run typecheck` 0 errors, `npm run build` 0 errors, all touched spec files green in isolation (component 5/5, tree helpers 3/3, dialog 6/6). A pre-existing, unrelated Karma suite instability (headless Chrome disconnects after a `SplitGenerationSwitcherComponent`/`ConfigDetailTagsSectionComponent` full-page-reload) surfaced during a full-suite run — not caused by this change, flagged for lot 7. Tracker: `docs/features/multirepo-bootstrap-d09-implementation-tracker.md` (lots 5-6 now `Done`; lot 7 — full validation + real MultiRepo run — remains).
- [2026-08-28] dotnet-dev — D01 fix (MultiRepo config-level repository PAT storage): mirrored the working project-level repository PAT flow (`AddProjectRepositoryCommandHandler`/`UpdateProjectRepositoryCommandHandler`) onto the config-level flow, one-for-one. Added `ProjectGitSecretNames.GetInfraConfigRepositoryPatSecretName(InfraConfigRepositoryId)`; `AddInfraConfigRepositoryCommand`/`UpdateInfraConfigRepositoryCommand` gained `PersonalAccessToken` (require+verify via `ProjectRepositoryConnectionVerifier`, reused as-is, no visibility change needed — same assembly); `RepositoryTargetResolver.BuildFromConfigRepository` now returns the real secret name instead of `PatSecretName: null` (the actual root cause of the connection-test/branch-listing/push failures in MultiRepo). Contracts (`Add/UpdateInfraConfigRepositoryRequest`) + `ProjectController` wired through. TDD: expanded `AddInfraConfigRepositoryCommandHandlerTests`/`UpdateInfraConfigRepositoryCommandHandlerTests` from 2 thin tests each to full happy-path/PAT-required/branch-verification-failure/incomplete-connection-details coverage (mirroring the reference handler tests exactly); no `RepositoryTargetResolverTests.cs` existed at all before this task despite the task brief assuming one did — created it fresh (`tests/InfraFlowSculptor.Application.Tests/Common/GitRouting/`) covering MultiRepo config-repo, no-repo-configured, and AllInOne project-repo resolution. Domain check confirmed `InfrastructureConfig.AddRepository`/`UpdateRepository` take non-nullable connection params (no slot/partial mode unlike `Project.AddRepository`), so `HasCompleteConnectionDetails` is unconditionally true for config repos reachable through the API — PAT is effectively always required on Add. Validation: solution build 0 errors; `Application.Tests` 1436 passed / 44 failed (unchanged pre-existing baseline, none in touched areas, confirmed by direct file inspection — none of the 44 are `Get<X>QueryHandlerTests`-adjacent to this area); full-solution `dotnet test` also shows pre-existing unrelated failures in Contracts.Tests/Infrastructure.Tests/Api.Tests already documented in `.claude/test-debt.md`'s 2026-08-28 entry, none caused by this change (verified the Contracts snapshot test only scans `.Responses` types, not the `.Requests` types touched here). D09/D10 (bootstrap/push MultiRepo gaps) and frontend PAT field wiring are separate, out of scope for this task.
- [2026-08-28] main-thread — Phase 2 (Tri) closed: all `feature-map.md` rows now carry a `keep`/`fix`/`cut` decision (10 remaining `?` rows resolved in one pass — F03 in particular verified directly in code, not left on an empty cartography note). Reopened and validated phase 0 per owner's choice before starting repair: `dotnet build` 0 errors, `dotnet test` 3827 tests/51 failures (44 = already-tracked 2026-06-03 debt, root cause confirmed as stale `Get<X>QueryHandlerTests` fixtures not matching the current handler shape, not a prod bug; 6 already-documented; 2 new small gaps logged), frontend `npm run typecheck` fixed (corrupted `node_modules/typescript`, missing `bin/`, forced targeted reinstall), Aspire AppHost startup confirmed clean (Postgres/Azurite/KeyVault emulator/DbGate all `Up`, 0 errors). Phase 3 (repair) can now be validated by execution, not just code reading. See `docs/stabilization/NEXT.md` and `.claude/test-debt.md`.
- [2026-08-28] main-thread — Stabilization phase 2 (Tri) started with the project owner: arbitration 1-3 confirmed. `MultiRepo` layout to be repaired (D01+D09+D10), not cut — confirmed product priority. D05/D08/D11 (the three user-facing lies) confirmed `fix`. Dead-code triage resolved per-resource rather than as one bucket: `NetworkingProfile` V2 (D04/F35) and the orphaned PAT dialog (F20) confirmed `cut` (both superseded by an existing path — V3 privatization, repo modal). `PrivateDnsZone` confirmed `cut` (superseded by V3 `PrivateEndpointDnsMode`). `NetworkSecurityGroup` and `FrontDoor` confirmed **kept** as future roadmap resources — not dead, deliberately demolished in the 2026-05-28 V1 cleanup, owner wants them supportable again; residues left untouched. See `docs/stabilization/feature-map.md` "Décisions de tri" and `.claude/memory/03-domain-model.md`.
- [2026-06-02] main-thread — Reworked the VNet address-space / DNS entry UX into a **segmented date-style masked input**. Replaced the two single-field DS primitives `ds-cidr-input` + `ds-ipv4-input` with ONE mode-driven component `app-ds-ip-input` (`mode: 'ipv4' | 'cidr'`): per-octet fields (+ prefix field for cidr), `.`/`/` separators rendered as a persistent visual mask, type-digits-only with focus auto-advance (octet full at 3 digits OR `value*10 > max`), Backspace/Arrow step-back, paste-to-distribute, CVA emits the unchanged `a.b.c.d` / `a.b.c.d/p` contract. Pure logic isolated in `ds-ip-input.util.ts` (getIpSegmentDefs/sanitizeSegment/shouldAdvanceSegment/joinSegments/splitToSegments) + types. Wired through `ds-list-input` (`inputType` switch now renders `app-ds-ip-input mode=...`) so BOTH the add-resource modal and resource-edit config inherit it with zero feature-screen template change; data layer (`addressSpacesInput`/`dnsServersInput` string[] + vnet-tag-input validators) untouched. Added `DS.IP_INPUT.OCTET_ARIA`/`PREFIX_ARIA` i18n (en/fr). Validation: util spec + component spec 31/31 Karma green, `tsc --noEmit` clean, full AOT `ng build` green. NOTE: local `node_modules/typescript` is missing its `bin/` dir — run tsc via `node node_modules/typescript/lib/tsc.js`; `node_modules/.bin/tsc` and `npm run typecheck` are broken until `npm install` is re-run.
- [2026-06-02] main-thread — Moved VNet **DDoS protection per-environment** end to end and fixed two silent generation gaps. Challenge: the 2026-05-28 add-VNet modal was internally contradictory — `commonForm` declared dead `vnetAddressSpacesInput`/`vnetDnsServersInput`/`enableDdosProtection` controls never rendered, the common-step section's subtitle over-promised address-range input while only showing a DDoS toggle, and the real `app-ds-list-input` address/DNS inputs lived one step later in `environments` (VNet is in `RESOURCE_TYPES_WITH_ENVIRONMENT_SETTINGS`). Backend slice: `EnableDdosProtection` relocated from `VirtualNetwork` aggregate to `VirtualNetworkEnvironmentSettings` (entity + `Create`/`Update`/`SetAllEnvironmentSettings` 4-tuple), contracts (`VirtualNetworkRequestBase` → `VirtualNetworkEnvironmentConfigEntry`, response env entry), Application (`VirtualNetworkEnvironmentConfigData`, Create/Update commands+handlers), EF config + migration `20260602153002_AddVnetEnvironmentDdosProtection` (drop column on VirtualNetworks, add on VirtualNetworkEnvironmentSettings), read repo now emits `enableDdosProtection` into per-env `EnvironmentConfigs`, Mapster mapping. **Generation gaps fixed:** (1) `ResourceTypeMetadata.GetBaseModuleName` had no `VirtualNetwork` case → returned `"unknown"`, so `ParameterFileAssembler.FindMatchingResource` never matched VNet modules and per-env overrides (addressPrefixes + DDoS) were silently dropped; (2) `VirtualNetworkTypeBicepGenerator.Generate()` omitted `addressPrefixes` from base `Parameters`, so the override key didn't exist to merge; (3) `ParameterFileAssembler.CoerceToOriginalType` couldn't emit array overrides — added `CoerceToBicepArray` (JSON string → `List<object>`) so per-env address spaces render as real Bicep arrays. Frontend: removed the bespoke common-step VNet section + dead controls/validators, added a per-env DDoS toggle next to address/DNS in both add-resource and resource-edit (env-settings helpers + form builders). Validation: Domain 641/641, BicepGeneration 1068/1068, Application VNet commands 22/22, Infrastructure VNet repo 8/8, frontend typecheck + AOT build + Karma 22/22 green. Pre-existing out-of-scope failures untouched (GetVirtualNetworkQueryHandler nav NRE, Contracts snapshot `Count` drift, `UpdateProjectEnvironmentRequest`). NOTE: stopped the running Aspire dev stack (AppHost/Api/Mcp) to release DLL locks for the build — restart with `dotnet run` on the AppHost.
- [2026-06-02] main-thread — Fixed `NullReferenceException` on resource detail endpoints (reported on Key Vault `GET /key-vault/{id}`). Root cause: resource repos overriding `GetByIdReadOnlyAsync` via a private `WithSubResources(...)` helper dropped the base-class `Include(ResourceGroup)`, so `Get<Resource>QueryHandler` threw on `resource.ResourceGroup!.InfraConfigId` during `VerifyReadAccessAsync`. Re-added `.Include(x => x.ResourceGroup)` on the read-only override (kept out of the shared helper used by the tracked write path) across KeyVault, WebApp, FunctionApp, AppServicePlan, ContainerApp, ContainerAppEnvironment, ContainerRegistry, CosmosDb, RedisCache, SqlServer, SqlDatabase, VirtualNetwork, DocumentIntelligence. Updated the 13 read-only repository tests (incl. `BaseRepositoryTests` via WebAppRepository) to seed a real `ResourceGroup` since the required FK makes the include an INNER JOIN; added a KeyVault regression test asserting the navigation loads. Validation: Infrastructure + repo tests green (only the 4 pre-existing StorageAccount orphan-fixture failures remain, unrelated); `RepositoryIncludeHelperConventionTests` green. Also fixed the 4 pre-existing `StorageAccountRepositoryTests` orphan-fixture failures (seed a real `ResourceGroup` via a private `NewResourceGroup()` helper); StorageAccount's repo code is intentionally left as-is — it correctly keeps `ResourceGroup` in the shared `WithSubResources` because `StorageAccountAccessHelper` reads `ResourceGroup.InfraConfigId` on both the tracked write and read-only paths. Full repository suite now green (173 pass, 9 Docker-gated skipped). NOTE: API must be restarted to pick up the fix (running instance held the DLL lock).
- [2026-05-30] `dev` — Privatization V3 feature complete (Lots 3-9): CQRS commands/API endpoints for PE config, 3 Bicep generation stages (`PrivateEndpointCompanionStage`, `NetworkingResolutionStage`, `PublicNetworkAccessTransformerStage`), `PrivateEndpointGroupIdCatalog` in GenerationCore, frontend networking section in resource-edit with DNS help dialog, V2 config-detail networking tab removed. Tracker: `docs/features/privatization-v3-implementation-tracker.md`. Validation: full build green, ~3827 tests green (1 pre-existing failure), frontend typecheck+build green.
- [2026-05-29] `dev`, `architect` — Assessed Stripe-based monetization gating against the current generation architecture: confirmed there is no billing/entitlement module in the repo yet, and recorded the layering guardrail that future Stripe webhook ingestion belongs in API/Application/Infrastructure while the domain should only carry local typed export-entitlement state.
- [2026-05-29] `dev`, `architect` — Analyzed the current authorization model versus real multi-tenancy: confirmed that the repo is still project-membership scoped (`ProjectAccessService` / `InfraConfigAccessService`) with no execution-scoped tenant context and no EF global tenant query filters. Recorded the constraint in auth/persistence memory to guide the future tenant-isolation design.
- [2026-05-29] `dev` — Started Privatization V3 with a TDD backend foundation: `AzureResource` now owns typed `PrivateEndpointConfiguration` + `PrivateEndpointDnsMode`, EF maps it as nullable owned columns on `AzureResource`, and migration `20260529090353_AddResourcePrivateEndpointConfiguration` adds the resource-level PE fields while keeping `IsPrivatized` for incremental compatibility. Tracker: `docs/features/privatization-v3-implementation-tracker.md`. Validation: full build green; Domain tests green (636); focused Infrastructure PE/string-length tests green (9; one pre-existing Testcontainers warning); full `dotnet test` still has the 5 known out-of-scope failures from `UpdateProjectEnvironmentRequestTests` and `SecurityMiddlewareIntegrationTests`.
- [2026-05-28] `dev` — Resolved the `DocumentIntelligence` vs V2 networking merge wave: kept `DocumentIntelligence` support across backend/frontend/contracts/tests, preserved `VirtualNetwork` + `NetworkingProfile` privatization work, and did not resurrect removed V1 networking resource types (`NetworkSecurityGroup`, `PrivateDnsZone`, `FrontDoor`). Validation: `runTests` green for the touched Application/Contracts snapshots (44 tests), `npm run typecheck` OK, `npm run build` OK.
- [2026-05-27] `dev` — Replaced the app CI Docker Buildx builder probe with a non-failing `docker buildx ls --format "{{.Name}}"` lookup after Azure DevOps PowerShell 5.1 surfaced `docker buildx inspect ifs-builder` as a terminating `NativeCommandError` when the builder was simply absent. Added a focused regression test in `AppPipelineWindowsShellCompatibilityTests`; `InfraFlowSculptor.PipelineGeneration.Tests` green.
- [2026-05-28] `dev`, `angular-front` — Redesigned the VNet entry UX end to end: add-resource and resource-edit now use `app-ds-tag-input` for address spaces and DNS servers, serialize through shared `vnet-tag-input.helpers.ts`, and open a shared `vnet-help-dialog` instead of relying on inline delimiter hints; the config-detail networking profile now exposes the same explicit help affordance. Validation: focused Karma specs green (20/20), `npm run typecheck` OK, `npm run build` OK.
- [2026-05-28] `dev`, `angular-front` — Fixed Virtual Network UX across add-resource and resource-edit: the add-resource common step now exposes DDoS, address-space, and optional DNS inputs and maps them into per-environment VNet settings; `resource-edit` now loads/saves `VirtualNetwork` through the singular `/virtual-network/{id}` route, hydrates per-environment address-space/DNS fields, and the config-detail networking profile now shows inline address-space/subnet-prefix guidance. Validation: focused Karma specs green (19/19), `npm run typecheck` OK, `npm run build` OK.
- [2026-05-28] `dev`, `angular-front` — Refined the config-detail Networking tab visual hierarchy: promoted the network mode into a top-level orchestration card, split VNet and DNS into distinct DS cards with clearer inset configuration bodies, and restyled resource privatization as a semantic operations list instead of a flat row stack. Validation: focused networking section Karma spec green (2/2), `npm run typecheck` OK, `npm run build` OK.
- [2026-05-28] `dev`, `angular-front` — Fixed the add-VNet frontend 404 in config-detail: `VirtualNetworkService` now matches the backend controller contract (`POST /virtual-network`, `GET /virtual-network/{id}`), removed the dead unused nested `/resource-groups/{id}/virtual-networks` service surface, and updated `add-resource-dialog` to pass the typed create request body directly. Validation: focused Karma specs green (4/4), `npm run typecheck` OK, `npm run build` OK.
- [2026-05-28] `dev` — Fixed duplicate cross-config reference creation when the UI retried/resent a VNet cross-config link: `AddCrossConfigReferenceCommandHandler` now reloads `InfrastructureConfig` through `GetByIdWithMembersAsync(...)` before calling the domain duplicate guard, so existing `CrossConfigReferences` are loaded and the handler returns `InfrastructureConfig.DuplicateCrossConfigReference` instead of leaking a PostgreSQL `23505` on `IX_CrossConfigResourceReferences_InfraConfigId_TargetResourceId`. Validation: focused handler tests green (8/8).
- [2026-05-28] `dev`, `angular-front` — V2 NetworkingProfile FULL IMPLEMENTATION COMPLETE: Domain aggregate (NetworkingProfile, NetworkingMode/VnetSource/DnsMode/CidrBlock value objects, environment overrides), Infrastructure (EF configs, repository, unique index), Application (SetNetworkingProfileCommand/Handler/Validator upsert, ToggleResourcePrivatizationCommand/Handler/Validator, GetNetworkingProfileQuery/Handler, NetworkingProfileResult DTO), Contracts (SetNetworkingProfileRequest, ToggleResourcePrivatizationRequest, NetworkingProfileResponse), API (3 Minimal API endpoints: GET+PUT /infra-config/{id}/networking-profile, PUT /resources/{id}/privatization), EF Migration (drops V1 tables, creates V2 tables, adds IsPrivatized to AzureResource), Bicep pipeline (3 skeleton stages: NetworkingResolution@520, PrivateEndpointCompanion@540, PublicNetworkAccess@560), Frontend (networking section tab in config-detail with mode/vnet/dns forms + per-resource privatization toggles, networking-profile service, i18n FR/EN). Also fixed: ValueObjectEqualityComponentsCoverageTests (EnumValueObject + CidrBlock support), ImportPreviewAnalyzer (unsupported adds ARM type not name), updated Contracts snapshot, added ToggleResourcePrivatizationCommandValidator. Build: 0 errors. Tests: all green except 5 pre-existing (4 SecurityMiddleware integration + 1 UpdateProjectEnvironmentRequest). Frontend: typecheck + build OK.
- [2026-05-28] `dev` — V1 privatization demolition COMPLETED: deleted 18 folders + ~37 files (NSG/PrivateDnsZone/FrontDoor aggregates, PE entities, all associated Application/Contracts/Infrastructure/Tests/Frontend/Bicep code). Cleaned AzureResource base model, AzureResourceBaseRepository, BicepArmTypeCatalog, ResourceCommandFactory, ResourceTypes, MCP tools, frontend metadata/enums/i18n. Fixed GoldenArmTemplateTests, RepositoryIncludeHelperConventionTests, ResourceCommandFactoryDispatchMappingTests, and frontend spec. Kept: VirtualNetwork aggregate (reduced), PrivateEndpointTypeBicepGenerator (recycle for V2 stage 540), PrivateEndpointGroupIdCatalog (V2 auto-derivation), PrivateEndpointNetworkPolicy enum (valid subnet config). Build: 0 errors. Typecheck: 0 errors. Tests: all green (291 Infra + 36 Api). Next: V2 NetworkingProfile implementation.
- [2026-05-28] `dev`, `angular-front` — Locked the add-resource type picker to a 3-column desktop grid so categories with only 2 resource cards no longer stretch to full width; local responsive fallbacks now switch to 2 columns below `64rem` and 1 column below `40rem`. Validation: touched SCSS has no diagnostics; full Angular build is currently blocked by unrelated `networking-tab` import errors.
- [2026-05-28] `dev` — Widened the add-resource type-picker dialog to `1120px` (`maxWidth: 96vw`) and switched its resource cards to a responsive auto-fit grid with local long-label wrapping, so resource names no longer overflow their cards and no horizontal scroll is needed. Validation: Angular build OK.
- [2026-05-28] `dev` — Investigation & design document for the privatization redesign (V2): single `NetworkingProfile` aggregate (3 modes), `IsPrivatized` flag, 3 new pipeline stages, demolishes 60% of current networking aggregates. Deliverable: `docs/features/privatization-redesign-2026-05-28.md`. No code changes.
- [2026-05-28] `dev`, `angular-front` — DS spinner redesigned (triangle animation + gradient), replaced all pulse-dot loading indicators with `app-ds-spinner`, added loader showcase to DS. Resource-group edit added to config-detail. User-assigned identity names now emitted in Bicep resource modules. Split-generation embedded file panel UX and toggle-button polish. DS-first rule enforcement hardened in agent documentation.
- [2026-05-27] `dev`, `angular-front` — Simplified the split generated-file explorer chrome: embedded `app-bicep-file-panel` no longer renders its workspace banner under the inner tabs, and embedded file rows regained horizontal inset so hover/active outlines no longer stick to the edges. Validation: focused BicepFilePanel spec green, Angular typecheck + build OK.
- [2026-05-27] `dev`, `angular-front` — Removed the left/right gutter around split generation file explorers by wrapping each embedded `app-bicep-file-panel` in a local full-bleed host inside `SplitGenerationSwitcherComponent`; loading/error/empty/bootstrap guide padding stays unchanged. Validation: Angular typecheck + build OK.
- [2026-05-27] `dev` — Raffiné visuellement la page génération (sidebar + board + split-switcher). Sidebar : séparateur header/nav, hover micro-interactions (back arrow translate, brand hover bg), active state glow + indicateur 3px, sections avec séparateurs discrets, icônes avec opacity progressive, scrollbar discrète. Board : hero border/shadow adoucis, summary cards avec hover border, label typographie raffinée. Split-switcher : container avec border+shadow propre, collapse button allégé, CTA copy espacement amélioré, eyebrow plus discret, chips moins opaques, loading/error/empty arrondis raffinés. Typecheck + build OK.
- [2026-05-27] `dev` — Durci la règle agentique frontend "DS-first" dans la flotte : `.github/copilot-instructions.md`, `.claude/agents/angular-front.md`, et `.claude/agents/dev.md` interdisent désormais explicitement de craft un composant/pattern UI ad hoc dans une feature quand le design system couvre déjà le besoin. Si le pattern manque, l'agent doit d'abord créer ou étendre un primitive réutilisable dans `src/Front/src/app/shared/components/ds/`. La mémoire agentique `11-agents-skills.md` a été alignée.
- [2026-05-27] `dev` — Harmonisé les onglets des pages de détail `config-detail`, `project-detail` et `resource-edit` : `app-ds-tabs` supporte désormais `[stretch]="true"` pour répartir les tabs uniformément, et chaque page enveloppe maintenant les onglets et leur contenu dans le même panneau visuel afin d'éviter une barre bleue isolée au-dessus du contenu. Validation : Angular typecheck + build OK.
- [2026-05-27] `dev`, `angular-front` — Résolu l'intégralité de la dette DS ouverte (6 items P2/P3). (1) Corrigé 5 specs Karma cassés par W4.3 : sélecteurs legacy → DS-aware (`app-ds-button[icon="..."] button`, `app-ds-icon-button[icon="..."] button`). (2) Créé primitive `app-ds-key-value-input` (ControlValueAccessor, DsKeyValueItem{key,value}, validators, chip display) + migré 3 fichiers W2 (config-detail-tags, project-detail-tags, add-project-environment-dialog — suppression mat-chip-set + FormControls manuels). (3) Créé primitive `app-ds-accordion` (expand/collapse, ARIA, icon, tone) + migré DNS tutorial custom-domains (0 mat-expansion-panel restant). (4) Purgé SCSS dead-classes de 5 fichiers. (5) Extrait `_bicep-syntax-palette.scss` shared partial utilisé par settings + bicep-file-panel. (6) Vérifié les specs W1 primitives (ds-spinner, ds-progress-bar, ds-tag-input, ds-menu) compilent et passent. Validation finale : typecheck 0 erreur, build vert. Dette restante : 2 mat-chip-set dans naming-template (exclus par design — cursor-placement UIs).
- [2026-05-27] `dev` — Created `upgrade-orchestrator` agent and two upgrade skills (`dotnet-upgrade`, `angular-upgrade`). The agent orchestrates all version bumps by reading release notes, detecting breaking changes, planning atomic migrations, and delegating execution. Registered in `copilot-instructions.md`, `dev.md` routing table, and `11-agents-skills.md`.
- [2026-05-27] `dev` — Delivered exhaustive design-system coverage audit at [audits/audit-design-system-2026-05-27.md](../../audits/audit-design-system-2026-05-27.md). Inventoried 27 existing DS primitives, identified ~84 non-conformant templates, 30+ `mat-spinner` usages, 66+ residual Material buttons, 5 duplicated tag-input patterns. Proposed 4 mandatory new DS primitives (N1 `ds-spinner`, N4 `ds-tag-input`, N5 `ds-progress-bar`, N6 `ds-menu`) + 3 optional (N2 `ds-dialog-shell`, N3 `ds-card-mat`, N7 `ds-accordion`). Planned 8-wave mergeable migration (W1→W8). Tracker opened at [docs/features/ds-migration-2026-05-tracker.md](../../docs/features/ds-migration-2026-05-tracker.md). No production code touched in this session — pure scoping deliverable.
- [2026-05-27] `dev` — Implemented full monitoring/logging/metrics stack: added Azure.Monitor.OpenTelemetry.AspNetCore with conditional UseAzureMonitor(), EF Core tracing instrumentation, LoggingBehavior MediatR pipeline (timing + metrics), ApplicationMetrics class (command/query counters, duration histograms, bicep/pipeline generation counters), PostgreSQL health check, LoggerMessage source-gen for persistence and git provider hot paths. Frontend: installed @microsoft/applicationinsights-web + Angular plugin, created AppInsightsMonitoringService with AngularPlugin router tracking, AppInsightsErrorHandler, updated environment interface and production config. All 4356 tests pass, full solution build + Angular typecheck + production build OK.
- [2026-05-26] `dev` — Created comprehensive monitoring/logging/metrics audit and learning documentation at `docs/monitoring/monitoring-best-practices.md`. Covers current state audit, Application Insights integration plan (backend Azure Monitor Distro + frontend JS SDK), structured logging with `[LoggerMessage]` source gen, custom metrics, distributed tracing, health checks, and phased implementation roadmap.
- [2026-05-26] `dev`, `dotnet-dev`, `angular-front` — Refactored the create-project wizard to merge repository configuration into the Layout step: added stateless `POST /git/verify-connection` endpoint, added PAT support to `CreateProjectWithSetup`, created shared `RepositoryConnectionFormComponent` with inline verify+branch-autocomplete flow, simplified wizard from 5 to 4 steps (Identity → Layout+Repos → Environments → Review). All 1474 tests pass, typecheck+build OK.
- [2026-05-26] `dev`, `angular-front` — Fixed the create-project wizard dark-modal contrast regression by replacing legacy wizard text/border/focus CSS variables with `--ifs-*` DS tokens, added focused style-regression specs, and revalidated frontend tests/typecheck/build.
- [2026-05-22] `dev`, `dotnet-dev`, `angular-front` — Optimized hot resource detail/list reads: added Container App and project-resource read repositories with targeted EF projections, switched handlers away from TPT aggregate materialization, added ProjectService project-resources cache/coalescing, and validated with full .NET tests/build plus Angular spec/typecheck/build.
- [2026-05-22] `dev`, `angular-front` — Re-aligned the app-pipeline stack selector and repository auto-detect CTA onto one responsive header row, restored manual stack selection visibility, and upgraded the detect button styling without changing the options grid behavior.
- [2026-05-22] `dev`, `aspire-debug` - Investigated slow resource detail/edit navigation: Aspire traces confirmed Container App detail latency is dominated by a broad EF TPT query, with Angular `resource-edit` fan-out amplifying perceived load time; recorded backend projection and frontend lazy-loading follow-ups.
- [2026-05-22] `dev` — Restored the repo-aware pipeline-option detection DI seam after an accidental revert: `DetectPipelineOptionsQueryHandler` now passes the factory-resolved git provider into `PipelineOptionDetectionService`, targeted infrastructure tests are green again, and the old `IGitProviderService` startup failure is gone.
- [2026-05-21] `dev`, `dotnet-dev`, `angular-front` — Delivered the stack-aware application-pipeline wave end to end (Domain/Application/frontend/shared templates), added MCP/import application-stack detection plus repo-aware pipeline-option detection, enforced living implementation trackers across agents, refreshed the snapshot seed SQL contract, and fixed the API Dockerfile repo-root build context.
- [2026-05-20] `dev`, `angular-front` — Consolidated the app delivery wave: restored Container App `SourceCodePath` build-context propagation, generated app PR pipelines, switched shared app YAML to Windows-compatible PowerShell with literal-block fixes, standardized Azure DevOps pipeline naming, validated bootstrap ARM/ACR service connections, kept Container App ACR service connections per environment, and polished Dockerfile picker plus push-dialog branch UX.
- [2026-05-19] `dev`, `dotnet-dev`, `angular-front`, `aspire-debug` — Finished repository-id strong typing and alias removal end to end, landed the dedicated ACR-pull UAI flow with UI polish, and fixed AppHost startup by realigning the Key Vault emulator Aspire package family.
- [2026-05-18] `dev`, `dotnet-dev`, `angular-front` — Expanded MCP draft/tooling and the project-creation guide, restored repository Git UX on generation screens, refreshed the live project snapshot, hardened post-squash schema repair and PAT/settings/dashboard UX, finalized DS autocomplete rollout, and recorded the standing EF migration-history policy.
- [2026-05-17] `dev`, `dotnet-dev`, `angular-front`, `audit-expert` — Landed PAT scopes plus `xmin` concurrency, Docker-image validation across compute resources, baseline-history sync, audit/test-debt updates, and restored the solution to a 4,210-test green baseline.
- [2026-05-16] `dev`, `angular-front`, `dotnet-dev`, `review-expert`, `vibe-coding-refractaire` — Refined custom-domain certificate/DNS flow, latest-generation UX, Azure DevOps mirror sync, Azure-first positioning, broad frontend polish, and Bicep readability/hardening.
- [2026-05-15] `dev`, `audit-expert`, `review-expert`, `angular-front`, `aspire-debug`, `dream` — Closed the privatization/networking and pipeline-step-options wave, fixed shared API/MCP migration and user provisioning crashes, cleaned committed `*.csproj.lscache`, restored generation/navigation UX, and revalidated targeted checks plus build/tests/Aspire.
- [2026-05-14] `dev`, `dream` — Delivered pipeline step options end to end, fixed the shared `vw_ResourceEnvironmentEntries` migration trap, revalidated build/tests/Aspire, and consolidated project memory.
- [2026-05-13] `dev`, `angular-front` — Consolidated the 2026-05 audit state: API/MCP hardening, domain/persistence/CQRS guardrails, generation fixes, frontend decomposition, and the triage baseline of 13 remaining open issues.
- [2026-05-12] `dev` — Completed the large PR #328 remediation wave across validators, detached reads, EF constraints, Bicep/pipeline hardening, route-name constants, request security limits, and the blue-presence frontend refresh.
- [2026-05-11] `dev`, `angular-front`, `architect` — Delivered enterprise UI refresh waves 4-7, DS polish, project/config UX refinements, multi-format app-settings import, PR #327 cleanup, and Bicep/RBAC/Key Vault fixes.
- [2026-04-30] `dev` — Consolidated coverage, Sonar, README, LF output normalization, Windows-first output cleanup, and parallel-worktree guidance.
- [2026-04-29] `dev` — Hardened MCP/import/runtime flows, moved MCP to HTTP under Aspire, expanded typed import logic, aligned Graphify/agent guidance, and tightened code-quality guardrails.
- [2026-04-28] `dev` — Delivered PAT authentication and MCP V1 over HTTP, plus Sonar/security fixes across generators, ZIP handling, fonts, and Docker assets.
- [2026-04-27] `dev` — Completed the Bicep staged-pipeline refactor, Builder+IR migration, and TDD/xUnit tooling milestone.
- [2026-04-26] `dev` — Polished the frontend with Dockerfile picker branding, sticky wizard footer, repo-scoped split ZIP downloads, split bootstrap preview, and MultiRepo gating in config-detail.
- [2026-04-25] `dev` — Added the create-project wizard V1, SplitInfraCode generation/push, bootstrap split, DS panel actions, generic UAI parameter, and the Dream lock workflow.
- [2026-04-24] `dev` — Rolled out Angular 21, the design system, global Material overrides, and restored `Common/` for SplitInfraCode generation.
- [2026-04-23] `dev` — Delivered the layout-driven multi-repo wave with audit fixes, SplitInfraCode dual-repo generation/push, bootstrap ADO, and frontend multi-repo UX.
- [2026-04-22] `copilot` — Delivered custom domains end to end, bootstrap ADO hardening, Container App health probes/grouped params, CAE LAW security, and SQL Server fixes.
- [2026-04-21] `copilot` — Hardened Bicep generation with secure params, output validation, LAW fallback, and artifact path sanitization.
- [2026-04-20] `copilot` — Standardized the infra release artifact flow.
- [2026-04-17] `copilot` — Switched `.bicepparam` filenames to environment short names.
- [2026-04-16] `copilot` — Sealed enum value objects, moved response IDs to strings, completed `.ProducesProblem(401)` coverage, normalized camelCase Bicep output, and decoupled API from Aspire.
- [2026-04-15] `copilot` — Merged `main` into the DDD branch and sealed AzureResource aggregates.
- [2026-04-14] `copilot` — Made audit scripts compatible with Windows PowerShell 5.1.
- [2026-04-13] `copilot` — Added draw.io tooling.
- [2026-04-04] `copilot` — Stabilized generation/pipelines, refactored `BicepAssembler`, and fixed FK cascade behavior.
- [2026-04-03] `copilot` — Added app pipeline generation and GitNexus integration.
- [2026-04-02] `copilot` — Completed the user-assigned identity refactor across layers.
- [2026-04-01] `copilot` — Applied Windows-first conventions and general-config cleanup.
- [2026-03-31] `copilot` — Delivered Event Hub namespace support and configuration-keys UX.
- [2026-03-30] `copilot` — Added ACR pull roles, variable groups, Unit of Work, and domain quality rules.


