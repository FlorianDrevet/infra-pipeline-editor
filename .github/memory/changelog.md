- 2026-05-16 — Positioning: commercial assessment concluded the repo fits best as an Azure-first infrastructure modeling product centered on Bicep + Azure DevOps + MCP, rather than a generic multi-cloud IaC platform.
# Changelog
- [2026-05-16] `dev`, `angular-front` — Fixed the generation-configuration layout/repository state sync so mono / split / multi selection updates immediately on click, repository add/edit/delete now invalidate the cached project snapshot before reload, `generation-config` receives a typed refreshed project payload to keep its summary cards aligned, focused ChromeHeadless coverage now covers optimistic preset behavior plus dialog/delete refresh flows, and frontend `typecheck` / `build` stayed green.
- [2026-05-16] `dev`, `angular-front` — Removed the left-sidebar collapse mechanic: deleted the chevron toggle, fixed the shell/sidebar width at `240px`, removed the obsolete sidebar state service plus dead i18n keys, added focused sidebar coverage, and revalidated the targeted ChromeHeadless spec plus frontend `typecheck` and `build`.
- [2026-05-16] `dev`, `angular-front` — Harmonized `SplitGenerationSwitcherComponent` so the nested `Bicep` / `Pipeline` / `Bootstrap` tabs now reuse the same visual language as the outer `Infra` / `Code` tabs, and wrapped each generation side in one continuous bordered shell attached to the rounded top navigation; frontend build stayed green.
- [2026-05-16] `dev`, `angular-front` — Removed the useless horizontal scrollbar from the topbar project/config search popup by moving the result-row button reset out of inline HTML and sizing the full-width rows with `box-sizing: border-box`; frontend `typecheck` and `build` stayed green.
- [2026-05-16] `dev` — Restored discoverability of user display preferences by adding a persistent topbar shortcut to `/settings`, fixed the navigation language-switch grouping semantics, added a focused ChromeHeadless spec for the settings entry point, and revalidated the targeted spec plus frontend `typecheck`.
- [2026-05-16] `dev`, `angular-front` — Polished the `resource-edit` app-settings tab so the info tip, action row, and grouped cards now sit inside a padded inner shell, switched the top actions to DS primary/secondary buttons, renamed the import CTA to the generic multi-format “Import from file”, added a focused Angular spec for the DS action shell, and revalidated targeted tests plus frontend `typecheck`/`build`.
- [2026-05-16] `dev`, `angular-front` — Fixed `add-resource-dialog` VNet creation by wiring `VirtualNetworkService.create(...)` in the submitter, made unsupported submitter types fail explicitly instead of closing the dialog on a false success, added a focused ChromeHeadless spec for the submitter, and revalidated the targeted frontend spec plus `npm run typecheck`.
- [2026-05-16] `dev`, `angular-front` — Color-coded the `config-detail` resource action clusters for faster scanning: add actions now use the lighter accent blue, edit actions use a deeper brand-blue tone, and delete actions are clearly destructive red in both the resource rows and the top-right config delete CTA; revalidated frontend `typecheck` and `build`.
- [2026-05-16] `dev` — Performance optimization for resource-edit page: parallelized role-definition fetching (Promise.all instead of sequential for-loop), started background loads before project await, eliminated redundant GET after PUT in onSave() by reusing the update response, parallelized project-resource fetch in loadAllResources, and added 30s TTL in-memory caches to InfraConfigService.getById() and ProjectService.getProject() to avoid re-fetching on navigation.
- [2026-05-16] `dev`, `angular-front` — Fixed `add-resource-dialog` so resource types without real per-environment fields now skip the empty environment step and submit directly from the common step, aligned `config-detail` missing-environment diagnostics with the same shared resource-type metadata, and revalidated focused ChromeHeadless specs plus frontend `typecheck` and `build`.
- [2026-05-16] `dev`, `angular-front` — Removed the raw IDs from the configuration and resource detail header banners, deleted the now-unused header-ID SCSS selectors, revalidated frontend `typecheck` and `build`, and tracked the missing page-level header spec harness in `.github/test-debt.md`.
- [2026-05-16] `dev`, `angular-front` — Harmonized modal cancel/primary footers globally by adding a targeted `mat-dialog-actions` rule that equalizes button widths, increases spacing, and stacks the pair on small screens, then revalidated frontend `typecheck` and `build` and tracked the remaining visual-regression debt for this shared footer pattern.
- [2026-05-16] `dev`, `angular-front` — Fixed the project/config pipeline variable-groups tables so their 5-column headers/rows now use an explicit 5-track grid with horizontal overflow instead of wrapping the last columns onto a second line, then revalidated frontend `typecheck` and `build`.
- [2026-05-16] `dev` — Nudged the `add-resource-dialog` scrollbar farther from the resource cards by restoring a stable scrollbar gutter and increasing the scroll container's right-side breathing room, then revalidated the frontend build.
- [2026-05-16] `dev` — Global scrollbar restyle: replaced browser-default scrollbar with a slim 6px dark-themed scrollbar applied globally via `styles.scss` (uses `--ifs-border-subtle` thumb, hover brightening to `--ifs-border-strong`, transparent track, Firefox `scrollbar-width: thin` support). Removed redundant inline scrollbar overrides from `add-resource-dialog` and `ds-select`. Enhanced `ifs-scrollbar-soft` mixin accordingly.
- [2026-05-16] `dev`, `angular-front` — Reordered the project sidebar `Générer` section so `Génération` now appears before `Configuration`, removed the duplicate top-level `Générer toutes les configurations` CTA from `/projects/:id/generate` while keeping the ready-state CTA, aligned the sidebar spec with the dedicated Members/Settings routes, and revalidated frontend typecheck/build plus focused ChromeHeadless specs with two unrelated pre-existing `generation-board` spec failures still open.
- [2026-05-16] `dev` — Major navigation refactor: extracted Members and Settings from project-detail sub-tabs into standalone pages (`/projects/:id/members`, `/projects/:id/settings`), moved Tags into a new "Définir" sub-tab in project-detail, created `ProjectMembersComponent` (GitHub/SonarQube-style table UI) and `ProjectSettingsComponent` (Agent Pool + upcoming cards), updated sidebar routing, i18n (fr+en), detail-route-tabs enum/tests, and cleaned dead code from project-detail.
- [2026-05-16] `dev`, `angular-front` — Fixed the `project-detail` Agent Pool draft lifecycle so toggling the custom-pool switch off then on no longer clears the unsaved pool name, added focused helper coverage for that regression, and revalidated the targeted ChromeHeadless spec plus `npm run typecheck`.
- [2026-05-16] `dev`, `angular-front` — Polished the shared DS toggle off/on states so the thumb stays clearly visible when off and picks up a restrained accent-tinted spring-like transition when toggled on, then revalidated frontend `typecheck` and `build`.
- [2026-05-16] `dev`, `angular-front` — Fixed config-context `Génération` navigation so the sidebar now opens the canonical project generation page, kept `/config/:id/generate` as a legacy redirect bridge by resolving the owning project, added focused sidebar/config-generation specs, and revalidated frontend tests plus `npm run typecheck` and `npm run build`.
- [2026-05-16] `dev`, `angular-front` — Migrated the `project-detail` Agent Pool settings card to DS controls, moved the toggle switch to the right of its label, hid the Save action until the normalized pool value is actually dirty, added focused helper coverage for the dirty-state logic, and revalidated the targeted frontend spec plus `npm run typecheck`.
- [2026-05-16] `dev`, `angular-front` — Reordered the `project-detail` configuration row so the chevron stays next to the configuration name and the delete action sits at the far right, then revalidated frontend `typecheck` and `build`.
- [2026-05-16] `dev`, `angular-front` — Centered the resource-type icons in the naming abbreviations tables for `config-detail` and `project-detail` by flex-aligning the type cell and wrapping the label for safe ellipsis, with frontend `typecheck` and `build` green.
- [2026-05-16] `dev` — Fixed mojibake in generated `main.bicep` section comments by switching `MainBicepDeploymentSectionAssembler` headers to ASCII-only text, added focused assembler coverage, and revalidated the Bicep test project plus full solution tests/build after stopping local API/MCP/AppHost locks.
- [2026-05-16] `dev`, `angular-front` — Fixed the recurring frontend icon/title drift in shared DS page and section headers by aligning icons on a dedicated title row above subtitles, tightened the `project-detail` Settings-tab title heading alignment for labels like Tags and Agent Pool, added focused Angular specs for both DS headers, and revalidated the two ChromeHeadless specs plus `npm run build`.
- [2026-05-16] `dev`, `angular-front` — Made the project environment edit dialog fit small screens again: the modal now uses a scrollable body with a compact aligned footer, equalized Cancel/Save actions, and a focused Angular spec plus typecheck/build revalidation.
- [2026-05-16] `dev`, `angular-front` — Unified the remaining frontend toggles on the design system: the project environment dialog, config naming inheritance banner, and project agent-pool setting now all use `app-ds-toggle`, stale `MatSlideToggleModule` imports/legacy spacing were removed, focused Angular specs were added where practical, and the remaining page-level project-detail coverage gap was tracked in `.github/test-debt.md`.
- [2026-05-16] `dev` — Added more air to the `project-detail` environments UI: the header hero now sits farther from the lower cards, and the `Environment : dev` tag pill gained more inset; focused environment-section spec and frontend build are green.
- [2026-05-15] `dev`, `audit-expert`, `review-expert`, `angular-front`, `aspire-debug`, `dream` — Closed the privatization/networking and pipeline-step-options wave end-to-end, fixed the shared API/MCP migration gap and the `UserProvisioningService` raw-SQL provisioning crash, reviewed PR `#396` and cleaned committed `*.csproj.lscache`, restored URL-driven sidebar and project-generation flows, moved layout/repository editing to `/projects/:id/generate`, refreshed project environments and generated-file viewer UX/theme, and revalidated targeted frontend/infrastructure checks plus build/tests/Aspire.
- [2026-05-14] `dev`, `dream` — Delivered pipeline step options end-to-end, fixed the shared `vw_ResourceEnvironmentEntries` migration trap in `20260514104001_SyncPendingModelChanges`, revalidated full build/tests/Aspire, and consolidated project memory.
- [2026-05-13] `dev`, `angular-front` — Audit 2026-05 execution was consolidated into the durable state: `audits/audit-13-05-2026.md` recorded `66` findings / `65` GitHub issues, `audits/correction-plan-13-05-2026.md` drove the P0–P4 remediation waves, and `audits/triage-2026-05-13.md` is now the source of truth with `13` remaining open issues. Durable outcomes captured in thematic memory include API/MCP hardening (`traceId`, PAT usage throttling, coverage collector, MCP headers/rate limiting/HTTP warning, additive cancellation propagation), domain/persistence/CQRS guardrails (`AzureResource` encapsulation, `IUserProvisioningService`, domain events, detached read defaults, EF length/include guardrails, validator and DTO snapshot tests), generation fixes (multi-ACR existing-resource resolution, existing-resource API-version coverage, representative Bicep CI validation, narrower pipeline exception remapping), and frontend/UI refactors (enterprise shell/sidebar/footer refresh, generation board page, shared resource metadata, large-component decomposition, build-budget fix).
- [2026-05-12] `dev` — Large PR #328 remediation wave: validators and command guardrails (`APP-001`), read-only repository splits and EF constraints (`DB-001` / `DB-003` / `DB-006`), Bicep/pipeline hardening (`GEN-*`), route-name constants, request limits/CORS/rate limiting/tag/path security, triage resync, and blue-presence frontend theme refresh; end-of-day full solution build and tests were green.
- [2026-05-11] `dev`, `angular-front`, `architect` — Frontend enterprise UI refresh waves 4-7, DS polish, project/config-detail UX refinements, import-app-settings multi-format, PR #327 Sonar/review cleanup, and Bicep/RBAC/Key Vault fixes; targeted frontend/backend validations were green.
- [2026-04-30] `dev` — Coverage/Sonar/README consolidation: expanded tests, LF output normalization, GitHub landing-page rewrite, Windows-first output cleanup, and refreshed parallel-worktree guidance.
- [2026-04-29] `dev` — MCP/import/runtime hardening plus Graphify/agent alignment: HTTP MCP under Aspire, typed import flows, dependency expansion/default normalization, PAT docs, and stricter code-quality guardrails.
- [2026-04-28] `dev` — PAT authentication and MCP V1 over HTTP delivered, plus Sonar/security quick wins across generators, ZIP handling, fonts, and Docker assets.
- [2026-04-27] `dev` — Bicep staged pipeline refactor, Builder+IR migration, and TDD/xUnit tooling milestone completed.
- [2026-04-26] `dev` — Frontend polish: Dockerfile picker brand fix, sticky wizard footer, repo-scoped split ZIP downloads, split bootstrap preview, and MultiRepo gating in config-detail.
- [2026-04-25] `dev` — Create-project wizard V1, SplitInfraCode generation/push, bootstrap split, DS panel actions, generic UAI parameter, and Dream lock workflow.
- [2026-04-24] `dev` — Angular 21 plus design-system rollout, global Material overrides, and `Common/` restore for SplitInfraCode generation.
- [2026-04-23] `dev` — Multi-repo/layout-driven repository wave with audit fixes, SplitInfraCode dual-repo generation/push, bootstrap ADO, and frontend multi-repo UX.
- [2026-04-22] `copilot` — Custom domains end-to-end, bootstrap ADO hardening, Container App health probes and grouped params, CAE LAW security, and SQL Server fixes.
- [2026-04-21] `copilot` — Bicep hardening: secure params, output validation, LAW fallback, and artifact path sanitization.
- [2026-04-20] `copilot` — Infra release artifact flow standardized.
- [2026-04-17] `copilot` — `.bicepparam` filenames switched to environment short names.
- [2026-04-16] `copilot` — Sealed enum value objects, string response IDs, `.ProducesProblem(401)` coverage, camelCase Bicep normalization, and API/Aspire decoupling.
- [2026-04-15] `copilot` — `main` merged into the DDD branch and AzureResource aggregates sealed.
- [2026-04-14] `copilot` — Audit scripts made Windows PowerShell 5.1 compatible.
- [2026-04-13] `copilot` — draw.io tooling added.
- [2026-04-04] `copilot` — Generation/pipeline stabilization, BicepAssembler refactor, and FK cascade fixes.
- [2026-04-03] `copilot` — App pipeline generation and GitNexus integration.
- [2026-04-02] `copilot` — User-assigned identity refactor completed across layers.
- [2026-04-01] `copilot` — Windows-first conventions and general-config cleanup.
- [2026-03-31] `copilot` — Event Hub namespace and configuration-keys UX delivered.
- [2026-03-30] `copilot` — ACR pull roles, variable groups, Unit of Work, and domain quality rules added.
- [2026-03-29] `copilot` — Azure DevOps pipeline YAML generation added.
- [2026-03-28] `copilot` — Unified generation UX, mono-repo pipeline, and architect agent introduced.
- [2026-03-27] `copilot` — UAI grouping and Storage Account CORS/lifecycle work delivered.
- [2026-03-26] `copilot` — Storage Account CORS UX plus queue/table Bicep support added.
- [2026-03-24] `copilot` — Log Analytics Workspace and Application Insights end-to-end delivered.
