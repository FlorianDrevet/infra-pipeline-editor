# Audit 2026-04 — Triage 2026-05-12

> Triage exhaustif des findings de l'audit 2026-04 réalisé sur PR #328 (`refacto/audit-12-05`).
> Établit pour chaque issue ouverte l'état réel, l'action retenue, et la justification courte.
>
> **État avant triage** : 117 issues ouvertes
> **Snapshot GitHub 2026-05-13** : 0 issue encore ouverte (cf. `gh issue list --repo FlorianDrevet/infra-pipeline-editor --state open`)
> **Reste réellement à traiter** : 0 issue d'ingénierie. Aucun reliquat GitHub audit ne reste après la fermeture manuelle du lot final `FIXED-IN-PR` sur cette branche.
>
> **Légende** :
> - 🟢 **FIXED-IN-PR** : corrigé dans cette PR, fermeture automatique au merge via `Closes #N`
> - 🟡 **CLOSED-OBSOLETE** : déjà résolu par un commit antérieur (vérifié dans le code)
> - 🟡 **CLOSED-DUPLICATE** : doublon de re-sync `audit-workflow`, fermé en faveur du canonique
> - 🟡 **CLOSED-MISGUIDED** : finding incorrect au regard des contraintes techniques du projet
> - 🔴 **OPEN-ACTIONABLE** : finding valide, scope court, prêt à implémenter
> - 🔴 **OPEN-LARGE** : finding valide, refactoring important, nécessite une PR dédiée
> - 🔴 **OPEN-NEEDS-DESIGN** : finding valide mais demande une décision d'architecture préalable

---

## Récapitulatif fermetures déjà réalisées (T0 → T1)

| Catégorie | Nombre | Détail |
|-----------|-------:|--------|
| Doublons fermés ([AUDIT][XXX] vs [XXX]) | 51 | Re-sync `audit-workflow` 2026-04-15 |
| Obsolètes fermés batch 1 | 4 | #162 SEC-001, #171 DDD-001, #198 DDD-011, #241 [AUDIT][DDD-002] |
| Obsolètes fermés batch 2 | 2 | #173 GEN-001, #184 APP-004 |
| Obsolètes fermés batch 3 | 2 | #177 SEC-007, #191 GEN-003 |
| Misguided fermés | 2 | #221 API-008, #216 DDD-013 |
| Fermées sur GitHub depuis le resync 2026-05-13 | 57 | #164 SEC-003, #165 SEC-004, #167 DB-002, #168 APP-001, #170 APP-003, #178 DB-003, #179 DB-004, #180 DB-005, #181 DB-006, #183 DB-008, #185 APP-005, #186 APP-006, #189 DDD-004, #190 DDD-005, #193 DDD-006, #194 DDD-007, #195 DDD-008, #196 DDD-009, #199 APP-008, #200 APP-009, #201 APP-010, #202 APP-011, #203 APP-012, #204 API-001, #205 API-002, #207 API-004, #213 INFRA-001, #214 INFRA-002, #217 DDD-014, #223 APP-014, #227 INFRA-004, #295 [AUDIT][INFRA-003], #329 SEC-008, #206 API-003, #224 GEN-009, #243 [AUDIT][GEN-002], #163 SEC-002, #166 DB-001, #169 APP-002, #175 SEC-005, #176 SEC-006, #182 DB-007, #187 APP-007, #188 DDD-003, #192 GEN-004, #197 DDD-010, #208 API-005, #209 GEN-005, #210 GEN-006, #211 GEN-007, #212 GEN-008, #215 DDD-012, #218 DDD-015, #219 API-006, #220 API-007, #222 APP-013, #225 GEN-010 |
| **Total fermées** | **118** | |

---

## Snapshot GitHub 2026-05-13

| Bucket | Nombre | Détail |
|--------|-------:|--------|
| Issues GitHub encore ouvertes | 0 | aucun reliquat audit ouvert après fermeture manuelle du dernier lot `FIXED-IN-PR` (`#163`, `#166`, `#169`, `#175`, `#176`, `#182`, `#187`, `#188`, `#192`, `#197`, `#208`, `#209`, `#210`, `#211`, `#212`, `#215`, `#218`, `#219`, `#220`, `#222`, `#225`) |
| Déjà corrigées sur la branche | 0 | aucun `FIXED-IN-PR` restant ouvert |
| À requalifier / probablement false-positive | 0 | aucune requalification immédiate restante après fermeture de `#196`, `#205`, `#213`, `#214` |
| **Vrai backlog restant** | **0** | aucun finding d'ingénierie ouvert restant sur la branche |

---

## Corrigées dans cette PR

| # | Code | Fix | Commit |
|---|------|-----|--------|
| #163 | SEC-002 | Headers de sécurité étendus (CSP `default-src 'none'`, COOP, CORP) | `feat(api): tighten CORS allow-list and add CSP/COOP/CORP headers` |
| #164 | SEC-003 | Rate limiting API modernisé: options typées validées au startup, policy globale native + policy `Expensive`, header `Retry-After`, projet de tests API dédié | `fix(api): modernize native rate limiting configuration` |
| #165 | SEC-004 | Validation `SafeRelativePath.TryNormalize(...)` ajoutée sur les endpoints config `GetBicepFileContent` et `GetPipelineFileContent`, avec tests dédiés sur les endpoints config et projet | `fix(api): validate generated artifact file paths` |
| #166 | DB-001 | Sweep DB-001 finalisé: contraintes explicites + migrations sur le noyau projet/infra-config/naming, `ResourceGroup.Name = 90`, puis matrice `ParameterDefinition` (`Name = 100`, `Type = 20`, `DefaultValue = 500`) verrouillée par tests EF dédiés | `fix(infrastructure): constrain parameter definition string columns` |
| #168 | APP-001 | Couverture validators complétée sur toutes les commandes Application restantes avec la convention durable `validator partout, mais input-only`; un test structurel `AllCommandsHaveValidatorsTests` verrouille désormais l'absence de régression et deux suites ciblées couvrent les premiers validators non triviaux (`UpdateFunctionApp`, `CreateStorageAccount`). | `(current slice)` |
| #175 | SEC-005 | CORS allow-list explicite (méthodes/headers/origines configurables) | (idem) |
| #176 | SEC-006 | Limite explicite de taille des corps de requête via `ApiRequestLimitsOptions` + `AddApiRequestLimits(...)` projetée sur `KestrelServerOptions.Limits.MaxRequestBodySize`, avec tests dédiés | `fix(api): configure explicit request body size limits` |
| #178 | DB-003 | Sweep detached/read-only finalisé sur les derniers query handlers et helpers de lecture: `GetDependentResources`, `ListAppSettings`, `CheckKeyVaultAccess`, `GetAvailableOutputs`, `SearchCodeRepoFiles`, `ListCodeRepoBranches`, `ListCustomDomains`, `GetSecureParameterMappings`, `CheckAcrPullAccess`, plus `AppPipelineRequestFactory` et `ApplicationFolderNameResolver`, avec suites ciblées dédiées et grep mécanique à zéro call tracké sur les fichiers visés. | `(current slice)` |
| #185 | APP-005 | La décomposition résiduelle des handlers projet est maintenant close via deux services project-scoped ciblés: `IProjectPipelineAggregator` extrait la génération mono-repo pipeline par configuration, la déduplication des environnements et l'assemblage final hors de `GenerateProjectPipelineCommandHandler`, tandis que `IMonoRepoBlobUploadOrchestrator` extrait le mapping d'upload Bicep/pipeline hors de `GenerateProjectPipelineCommandHandler` et `GenerateProjectBicepCommandHandler`; la couverture directe comprend les deux nouvelles suites de service, le slice handler Application, et la surface MCP impactée indirectement. | `(current slice)` |
| #201 | APP-010 | `ValidationBehavior` ignore désormais les queries et n'applique la validation qu'aux `ICommandBase`, avec tests ciblés | `fix(application): restrict validation behavior to commands` |
| #207 | API-004 | `EnumValidation` accepte désormais les noms d'enum string en case-insensitive, avec extension des tests existants | `fix(contracts): make enum validation case-insensitive` |
| #208 | API-005 | Limites Azure des tags centralisées dans `TagRequestConstraints` et cardinalité `Tags <= 15` verrouillée par `MaxCollectionCountAttribute` sur les 4 requêtes exposant des tags, avec tests contrats dédiés | `fix(contracts): enforce Azure tag count limits on requests` |
| #186 | APP-006 | Sweep des `.First()` côté Application: handlers/helpers user-facing durcis avec `FirstOrDefault()` + garde, `AddRoleAssignmentCommandHandler` sécurise la résolution post-persistence via fallback sur l'agrégat source muté, avec tests ciblés | `(pending current slice)` |
| #190 | DDD-005 | `AzureResource.AddDependency` rejette désormais les dépendances hors resource group et les cycles, tout en conservant le no-op sur doublon, avec tests domaine dédiés | `(pending current slice)` |
| #189 | DDD-004 | Les navigations EF exposées publiquement qui peuvent être absentes hors eager loading sont désormais nullable explicites (`AzureResource.ResourceGroup`, `ProjectEnvironmentDefinition.Project`) et le cas jumeau `ProjectMember.Project` a été aligné dans la même passe, avec réparation locale des déréférencements garantis par `Include` dans `ProjectRepository`. | `(current slice)` |
| #193 | DDD-006 | `Name` rejette maintenant `null` / vide / whitespace et `EntraId` rejette `Guid.Empty`, avec tests domaine dédiés; la règle reste volontairement locale à ces deux value objects et n'est pas remontée dans `SingleValueObject<T>` pour préserver les sentinelles métier encore utilisées ailleurs comme `SubscriptionId`. | `(current slice)` |
| #194 | DDD-007 | Une couture minimale de domain events in-process est désormais en place : `AggregateRoot<TId>` implémente `IHasDomainEvents`, `Project.Create(...)` lève `ProjectCreatedDomainEvent`, et `ProjectDbContext.SaveChangesAsync(...)` collecte, clear puis dispatch les events via `IDomainEventDispatcher` après un save réussi, avec tests domaine et infrastructure dédiés. | `(current slice)` |
| #195 | DDD-008 | La duplication `Name` + `Location` est maintenant centralisée dans `AzureResource.SetNameAndLocation(...)`; les agrégats concrets Azure conservent leurs signatures `Update(...)` publiques mais délèguent le tronc commun au helper de base, avec couverture domaine ciblée sur plusieurs agrégats représentatifs. | `(current slice)` |
| #199 | APP-008 | Le finding a été clos par preuve : la branche applique déjà un standard cohérent où les lectures restent masquées en not-found et les écritures préservent `Forbidden` pour les rôles insuffisants; des tests ciblés `ProjectAccessService` et `InfraConfigAccessService` verrouillent désormais explicitement cette sémantique sans réécriture large des services d'accès. | `(current slice)` |
| #200 | APP-009 | `IRepository<T>` expose désormais une surcharge additive `GetAllAsync(CancellationToken, params includes)` et `BaseRepository` la relaie jusqu'à `ToListAsync(cancellationToken)` tout en conservant la signature historique `GetAllAsync(params includes)` pour compatibilité avec les repos spécialisés comme `IUserRepository`, avec couverture ciblée Application/Infrastructure. | `(current slice)` |
| #202 | APP-011 | Clos par preuve : le pattern de domain service cross-cutting existe déjà avec `IRoleAssignmentDomainService` / `RoleAssignmentDomainService`, câblé en DI et réutilisé par les handlers role-assignment. | `(current slice)` |
| #203 | APP-012 | Clos par décision architecturale : la convention repository reste explicite (`GetByIdWithXAsync`, `GetByContainedXIdAsync`, variantes read-only documentées) et ne sera pas remplacée par une surcharge générique `IRepository` à fort blast radius. | `(current slice)` |
| #217 | DDD-014 | Clos via une remédiation plus ciblée que la recommandation d'audit : `AppServicePlan.OsType` était déjà typé et le vrai risque transverse restant était l'acceptation de `resourceType` arbitraires au niveau projet, désormais verrouillée par validation contre `AzureResourceTypes.All`; le rollout de nouveaux value objects leaf-level storage n'a pas été retenu. | `(current slice)` |
| #223 | APP-014 | Clos par preuve : la convention de nommage repository est déjà documentée dans `docs/architecture/persistence.md` et la branche contient désormais un slice de référence conforme avec `GetByContainedResourceIdAsync(...)`. | `(current slice)` |
| #227 | INFRA-004 | Clos par preuve : `ProjectDbContext` expose déjà `DbSet<AzureResource> AzureResources` pour les requêtes polymorphes TPT. | `(current slice)` |
| #295 | [AUDIT][INFRA-003] | Clos par preuve : la branche emploie déjà des projections légères ciblées, des méthodes de summary et des read repositories dédiés (`InfrastructureConfigReadRepository`) plutôt qu'une couche DTO générique sur tous les repositories. | `(current slice)` |
| #170 | APP-003 | `ListCrossConfigReferencesQueryHandler` batch-load désormais les configs cibles via `GetConfigSummariesByIdsAsync(...)` au lieu d'appeler `GetByIdAsync()` dans une boucle, avec tests application + repository ciblés | `fix(application): batch target config summaries for cross-config references` |
| #215 | DDD-012 | Override `SingleValueObject<T>.ToString()` + 4 tests xUnit | `feat(domain): override SingleValueObject<T>.ToString()` |
| #218 | DDD-015 | Suppression XML doc dupliquée sur `RemoveAppSetting` | `docs(domain): drop duplicate XML summary` |
| #219 | API-006 | Rename `KeyVaultControllerController` → `KeyVaultController` | `fix(api): rename KeyVaultControllerController` |
| #220 | API-007 | Rename `UseKeyVaultControllerController` → `UseKeyVaultController` | (idem) |

---

## Lot FIXED-IN-PR fermé sur GitHub le 2026-05-13 (21) — historique

### 🔴 CRITICAL severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #163 | SEC-002 | 🟢 FIXED-IN-PR | (voir ci-dessus) — la severité reste CRITIQUE car le label sera supprimé au merge |
| #166 | DB-001 | 🟢 FIXED-IN-PR | Sweep DB-001 désormais complet dans la PR #328 : contraintes explicites + migrations sur `Projects`, `ProjectEnvironments`, `InfrastructureConfigs`, `AzureResource`, `ProjectResourceNamingTemplates`, `ResourceNamingTemplates`, `ResourceGroup.Name` (`90`, règle Azure officielle Microsoft.Resources/resourcegroups`) et maintenant `ParameterDefinition` (`Name = 100`, `Type = 20`, `DefaultValue = 500`). La matrice `ParameterDefinition` est verrouillée par `CoreStringLengthConfigurationTests`; aucun alignement validator/contract supplémentaire n'était requis car l'entité ne possède pas encore de surface API/Application dédiée d'écriture. |
| #169 | APP-002 | 🟢 FIXED-IN-PR | Le périmètre du finding est désormais couvert dans la PR #328 : `GenerateBicepCommandHandler` / `DownloadBicepCommandHandler` étaient déjà protégés, `GeneratePipelineCommandHandler` / `DownloadPipelineCommandHandler` vérifient maintenant `VerifyWriteAccessAsync(...)` / `VerifyReadAccessAsync(...)` avant tout accès config/blob, et `CreateProjectCommandHandler` / `CreateProjectWithSetupCommandHandler` conservent la création self-service pour l'utilisateur authentifié tout en convertissant un échec de `ICurrentUser.GetUserIdAsync(...)` en réponse `Unauthorized` explicite au lieu d'un 500 générique. |

### 🟠 HIGH severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #175 | SEC-005 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #176 | SEC-006 | 🟢 FIXED-IN-PR | `ApiRequestLimitsOptions` + `AddApiRequestLimits(...)` configurent désormais explicitement `KestrelServerOptions.Limits.MaxRequestBodySize` avec fallback à 50 MB et validation startup; couverture ajoutée dans `RequestLimitsServiceCollectionExtensionsTests`. |
| #182 | DB-007 | 🟢 FIXED-IN-PR | Déjà absorbé sur la branche courante : `StorageAccountRepository` centralise maintenant sa chaîne d'eager loading dans `WithSubResources(IQueryable<StorageAccount>)`, réutilisée par les variantes trackées et read-only au lieu de dupliquer les `Include(...)`. |
| #187 | APP-007 | 🟢 FIXED-IN-PR | Déjà absorbé sur la branche courante : `GeneratePipelineCommandHandler` charge maintenant le projet une seule fois via `GetByIdWithAllAndPipelineVariableGroupsAsync(...)`, et les tests ciblés vérifient qu'il n'appelle plus `GetByIdWithPipelineVariableGroupsAsync(...)` ni `GetByIdWithAllAsync(...)`. |
| #188 | DDD-003 | 🟢 FIXED-IN-PR | Déjà absorbé sur la branche courante : `CorsRule` encapsule ses listes internes derrière des vues `ReadOnlyCollection<string>` mises en cache (`AllowedOrigins`, `AllowedMethods`, `AllowedHeaders`, `ExposedHeaders`) au lieu d'exposer des collections mutables. |
| #192 | GEN-004 | 🟢 FIXED-IN-PR | Les noms de variable groups passent désormais par `PipelineVariableGroupNameHelper` dans les stages infra/app, avec validation du placeholder `{env}`, rejet des expressions `${{ ... }}`, et quoting YAML verrouillé par `PipelineVariableGroupSecurityTests`. |

### 🟡 MEDIUM severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #197 | DDD-010 | 🟢 FIXED-IN-PR | Déjà absorbé sur la branche courante : les anciennes vues calculées CORS ont été remplacées par `GetBlobCorsRules()` / `GetTableCorsRules()` avec cache réutilisé et verrouillé par `StorageAccountTests`, ce qui supprime l'allocation répétée signalée sur les anciennes projections filtrées. |
| #208 | API-005 | 🟢 FIXED-IN-PR | `TagRequest` utilise désormais des constantes partagées (`512` / `256`) et les collections `Tags` exposées par `SetProjectTagsRequest`, `SetInfraConfigTagsRequest`, `AddProjectEnvironmentRequest`, et `UpdateProjectEnvironmentRequest` sont bornées à `15` via `MaxCollectionCountAttribute`, avec tests contrats dédiés. |
| #209 | GEN-005 | 🟢 FIXED-IN-PR | `BicepArmTypeCatalog` centralise désormais les versions API et ARM types qualifiés partagés. `ResourceTypeMetadata`, `RoleAssignmentModuleTemplates`, `MainBicepAssembler`, `KvSecretsModuleAssembler`, les constantes locales des générateurs et les templates legacy encore exécutés routent tous vers ce catalogue; la suite `InfraFlowSculptor.BicepGeneration.Tests` reste verte. |
| #210 | GEN-006 | 🟢 FIXED-IN-PR | Première tranche technique désormais implémentée sur la branche courante : `BicepGenerationEngine`, `PipelineGenerationEngine`, et `AppPipelineGenerationEngine` retournent maintenant `ErrorOr<T>` pour les erreurs attendues de génération via la taxonomie partagée `GenerationErrors`, tandis que les handlers/Application propagent ces erreurs au lieu de laisser retomber `ArgumentException` / `InvalidOperationException` / `NotSupportedException` sur le handler global 500. Les stages et helpers internes restent volontairement exception-based pour les invariants/bugs. Validation verte sur `InfraFlowSculptor.PipelineGeneration.Tests`, sur les tests Application ciblés des handlers de génération, et sur les builds prod `BicepGeneration` + `Application`; la suite `InfraFlowSculptor.BicepGeneration.Tests` n'a pas pu être rejouée dans cette session à cause d'erreurs de compilation préexistantes hors-scope autour de `GeneratedTypeModule`. |
| #211 | GEN-007 | 🟢 FIXED-IN-PR | La normalisation camelCase est maintenant centralisée dans `GenerationCore.BicepIdentifierNormalizer`; `BicepIdentifierHelper`, `BicepFormattingHelper.SanitizeBicepKey`, `AppSettingPipelineParameterNameHelper` et `SecureParameterOverrideHelper` y délèguent. Couverture ciblée ajoutée côté GenerationCore, BicepGeneration et Application. |
| #212 | GEN-008 | 🟢 FIXED-IN-PR | `StorageAccountTypeBicepGenerator` mutualise désormais le parsing blob/table CORS via `ParseCorsRuleDataList(...)`, avec test ciblé sur les companions `Blobs` / `Tables`. |

### 🟢 LOW severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #215 | DDD-012 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #218 | DDD-015 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #219 | API-006 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #220 | API-007 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #222 | APP-013 | 🟢 FIXED-IN-PR | La duplication de récupération du dernier lot d'artefacts/blob est désormais centralisée dans `BlobDownloadHelper` via `GetLatestBlobFilesAsync(...)` et `GetLatestDualBucketBlobFilesAsync(...)`; `PushProjectArtifactsToMultiRepoCommandHandler` réutilise ce helper pour les artefacts pipeline et bootstrap au lieu de dupliquer la sélection du dernier bucket horodaté. |
| #225 | GEN-010 | 🟢 FIXED-IN-PR | `AppPipelineGenerationEngine` valide désormais `request.DeploymentMode` contre `DeploymentModes.All` avant le lookup du générateur, avec couverture ciblée `AppPipelineGenerationEngineValidationTests`. |

---

## Recommandations stratégiques pour la suite

### Priorité 1 — Quick wins TDD (PR dédiée 2 jours)
- Aucun quick win stale restant après resync local : `DDD-003`, `DDD-010`, `DB-007`, et `APP-007` sont déjà absorbés sur `refacto/audit-12-05`.

### Priorité 2 — Performance / DB (PR dédiée avec migrations)
- Aucune priorité DB dédiée immédiate après fermeture de `#183` comme décision de baseline release-engineering.

### Priorité 3 — Architecture (décisions à prendre)
- Aucune décision d'architecture d'audit encore ouverte sur cette branche : `#202`, `#203`, et `#227` ont été clôturées par preuve ou décision explicite.

### À fermer / resynchroniser côté GitHub
- Aucun reliquat GitHub : le lot final `FIXED-IN-PR` (`#163`, `#166`, `#169`, `#175`, `#176`, `#182`, `#187`, `#188`, `#192`, `#197`, `#208`, `#209`, `#210`, `#211`, `#212`, `#215`, `#218`, `#219`, `#220`, `#222`, `#225`) a été fermé manuellement le 2026-05-13.

### À requalifier / fermer après validation humaine
- Aucune requalification immédiate restante après fermeture de `#196`, `#205`, `#213` et `#214`.

---

## Suite immédiate

1. Le backlog audit d'ingénierie sur `refacto/audit-12-05` est à `0`.
2. Le backlog GitHub audit est lui aussi à `0` après fermeture manuelle du dernier lot `FIXED-IN-PR`.
3. Le prochain travail utile sur cette branche n'est plus une vague de backlog, mais la phase de review/merge.
