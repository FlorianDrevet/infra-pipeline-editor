# Audit 2026-04 — Triage 2026-05-12

> Triage exhaustif des findings de l'audit 2026-04 réalisé sur PR #328 (`refacto/audit-12-05`).
> Établit pour chaque issue ouverte l'état réel, l'action retenue, et la justification courte.
>
> **État avant triage** : 117 issues ouvertes
> **Après triage** : 45 issues ouvertes (cf. `gh issue list --state open`)
>
> **Légende** :
> - 🟢 **FIXED-IN-PR** : corrigé dans cette PR, fermeture automatique au merge via `Closes #N`
> - 🟡 **CLOSED-OBSOLETE** : déjà résolu par un commit antérieur (vérifié dans le code)
> - 🟡 **CLOSED-DUPLICATE** : doublon de re-sync `audit-workflow`, fermé en faveur du canonique
> - 🟡 **CLOSED-MISGUIDED** : finding incorrect au regard des contraintes techniques du projet
> - 🔴 **OPEN-ACTIONABLE** : finding valide, scope court, prêt à implémenter
> - 🔴 **OPEN-LARGE** : finding valide, refactoring important, nécessite une PR dédiée
> - 🔴 **OPEN-NEEDS-DESIGN** : finding valide mais demande une décision d'architecture préalable

---

## Récapitulatif fermetures déjà réalisées (T0 → T1)

| Catégorie | Nombre | Détail |
|-----------|-------:|--------|
| Doublons fermés ([AUDIT][XXX] vs [XXX]) | 51 | Re-sync `audit-workflow` 2026-04-15 |
| Obsolètes fermés batch 1 | 4 | #162 SEC-001, #171 DDD-001, #198 DDD-011, #241 [AUDIT][DDD-002] |
| Obsolètes fermés batch 2 | 2 | #173 GEN-001, #184 APP-004 |
| Obsolètes fermés batch 3 | 2 | #177 SEC-007, #191 GEN-003 |
| Misguided fermés | 2 | #221 API-008, #216 DDD-013 |
| **Total fermées** | **61** | |

---

## Corrigées dans cette PR (auto-close au merge)

| # | Code | Fix | Commit |
|---|------|-----|--------|
| #163 | SEC-002 | Headers de sécurité étendus (CSP `default-src 'none'`, COOP, CORP) | `feat(api): tighten CORS allow-list and add CSP/COOP/CORP headers` |
| #164 | SEC-003 | Rate limiting API modernisé: options typées validées au startup, policy globale native + policy `Expensive`, header `Retry-After`, projet de tests API dédié | `fix(api): modernize native rate limiting configuration` |
| #165 | SEC-004 | Validation `SafeRelativePath.TryNormalize(...)` ajoutée sur les endpoints config `GetBicepFileContent` et `GetPipelineFileContent`, avec tests dédiés sur les endpoints config et projet | `fix(api): validate generated artifact file paths` |
| #166 | DB-001 | Sweep DB-001 finalisé: contraintes explicites + migrations sur le noyau projet/infra-config/naming, `ResourceGroup.Name = 90`, puis matrice `ParameterDefinition` (`Name = 100`, `Type = 20`, `DefaultValue = 500`) verrouillée par tests EF dédiés | `fix(infrastructure): constrain parameter definition string columns` |
| #175 | SEC-005 | CORS allow-list explicite (méthodes/headers/origines configurables) | (idem) |
| #176 | SEC-006 | Limite explicite de taille des corps de requête via `ApiRequestLimitsOptions` + `AddApiRequestLimits(...)` projetée sur `KestrelServerOptions.Limits.MaxRequestBodySize`, avec tests dédiés | `fix(api): configure explicit request body size limits` |
| #201 | APP-010 | `ValidationBehavior` ignore désormais les queries et n'applique la validation qu'aux `ICommandBase`, avec tests ciblés | `fix(application): restrict validation behavior to commands` |
| #207 | API-004 | `EnumValidation` accepte désormais les noms d'enum string en case-insensitive, avec extension des tests existants | `fix(contracts): make enum validation case-insensitive` |
| #208 | API-005 | Limites Azure des tags centralisées dans `TagRequestConstraints` et cardinalité `Tags <= 15` verrouillée par `MaxCollectionCountAttribute` sur les 4 requêtes exposant des tags, avec tests contrats dédiés | `fix(contracts): enforce Azure tag count limits on requests` |
| #186 | APP-006 | Sweep des `.First()` côté Application: handlers/helpers user-facing durcis avec `FirstOrDefault()` + garde, `AddRoleAssignmentCommandHandler` sécurise la résolution post-persistence via fallback sur l'agrégat source muté, avec tests ciblés | `(pending current slice)` |
| #190 | DDD-005 | `AzureResource.AddDependency` rejette désormais les dépendances hors resource group et les cycles, tout en conservant le no-op sur doublon, avec tests domaine dédiés | `(pending current slice)` |
| #170 | APP-003 | `ListCrossConfigReferencesQueryHandler` batch-load désormais les configs cibles via `GetConfigSummariesByIdsAsync(...)` au lieu d'appeler `GetByIdAsync()` dans une boucle, avec tests application + repository ciblés | `fix(application): batch target config summaries for cross-config references` |
| #215 | DDD-012 | Override `SingleValueObject<T>.ToString()` + 4 tests xUnit | `feat(domain): override SingleValueObject<T>.ToString()` |
| #218 | DDD-015 | Suppression XML doc dupliquée sur `RemoveAppSetting` | `docs(domain): drop duplicate XML summary` |
| #219 | API-006 | Rename `KeyVaultControllerController` → `KeyVaultController` | `fix(api): rename KeyVaultControllerController` |
| #220 | API-007 | Rename `UseKeyVaultControllerController` → `UseKeyVaultController` | (idem) |

---

## Issues restantes ouvertes (45) — classification

### 🔴 CRITICAL severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #163 | SEC-002 | 🟢 FIXED-IN-PR | (voir ci-dessus) — la severité reste CRITIQUE car le label sera supprimé au merge |
| #164 | SEC-003 | 🟢 FIXED-IN-PR | Implémentation native ASP.NET Core modernisée dans `RateLimiting/` avec configuration typée centralisée, `Retry-After`, suppression du reliquat `LoginPolicy`, et tests dédiés dans `InfraFlowSculptor.Api.Tests`. |
| #165 | SEC-004 | 🟢 FIXED-IN-PR | Validation `SafeRelativePath.TryNormalize(...)` ajoutée sur les endpoints config `GetBicepFileContent` / `GetPipelineFileContent`; `ImportController` n'était pas concerné par le finding, et les endpoints projet étaient déjà protégés. |
| #166 | DB-001 | 🟢 FIXED-IN-PR | Sweep DB-001 désormais complet dans la PR #328 : contraintes explicites + migrations sur `Projects`, `ProjectEnvironments`, `InfrastructureConfigs`, `AzureResource`, `ProjectResourceNamingTemplates`, `ResourceNamingTemplates`, `ResourceGroup.Name` (`90`, règle Azure officielle Microsoft.Resources/resourcegroups`) et maintenant `ParameterDefinition` (`Name = 100`, `Type = 20`, `DefaultValue = 500`). La matrice `ParameterDefinition` est verrouillée par `CoreStringLengthConfigurationTests`; aucun alignement validator/contract supplémentaire n'était requis car l'entité ne possède pas encore de surface API/Application dédiée d'écriture. |
| #167 | DB-002 | 🟡 CLOSED-OBSOLETE | Finding d'audit devenu obsolète : les colonnes critiques citées sont déjà couvertes par le modèle relationnel actuel, soit explicitement (`InfrastructureConfig.ProjectId`, `AzureResource.ResourceType`), soit via indexes FK/conventions (`AzureResource.ResourceGroupId`, `ResourceGroup.InfraConfigId`, `AppSetting.SourceResourceId` / `KeyVaultResourceId`, `AppConfigurationKey.SourceResourceId` / `KeyVaultResourceId`), soit via le composite unique `RoleAssignment(SourceResourceId, TargetResourceId, UserAssignedIdentityId, RoleDefinitionId)`. Couverture verrouillée par `IndexCoverageConfigurationTests` sur modèle Npgsql relationnel ; aucune migration additionnelle requise. |
| #168 | APP-001 | 🔴 OPEN-LARGE | Le sweep reste large, mais la PR #328 couvre désormais un nouveau sous-ensemble sensible des commandes projet de push Git : `PushProjectBootstrapPipelineToGitCommandValidator` et `PushProjectGeneratedArtifactsToGitCommandValidator` ont été ajoutés et alignés sur les règles déjà appliquées à `PushProjectBicepToGitCommand` / `PushProjectPipelineToGitCommand` (`ProjectId` requis, `BranchName` borné et regex-safe, `CommitMessage` borné). Le finding reste ouvert pour le reste des commandes sans validator FluentValidation. |
| #169 | APP-002 | 🟢 FIXED-IN-PR | Le périmètre du finding est désormais couvert dans la PR #328 : `GenerateBicepCommandHandler` / `DownloadBicepCommandHandler` étaient déjà protégés, `GeneratePipelineCommandHandler` / `DownloadPipelineCommandHandler` vérifient maintenant `VerifyWriteAccessAsync(...)` / `VerifyReadAccessAsync(...)` avant tout accès config/blob, et `CreateProjectCommandHandler` / `CreateProjectWithSetupCommandHandler` conservent la création self-service pour l'utilisateur authentifié tout en convertissant un échec de `ICurrentUser.GetUserIdAsync(...)` en réponse `Unauthorized` explicite au lieu d'un 500 générique. |
| #170 | APP-003 | 🟢 FIXED-IN-PR | Le N+1 résiduel de `ListCrossConfigReferencesQueryHandler` est supprimé : les configs cibles sont désormais résolues en batch via `GetConfigSummariesByIdsAsync(...)`, puis jointes aux métadonnées de ressources déjà batchées. Couverture ajoutée côté application (`ListCrossConfigReferencesQueryHandlerTests`) et repository (`InfrastructureConfigRepositoryTests`). |

### 🟠 HIGH severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #175 | SEC-005 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #176 | SEC-006 | 🟢 FIXED-IN-PR | `ApiRequestLimitsOptions` + `AddApiRequestLimits(...)` configurent désormais explicitement `KestrelServerOptions.Limits.MaxRequestBodySize` avec fallback à 50 MB et validation startup; couverture ajoutée dans `RequestLimitsServiceCollectionExtensionsTests`. |
| #178 | DB-003 | 🔴 OPEN-LARGE | Remédiation partielle poursuivie dans la PR #328 : `ProjectAccessService.VerifyReadAccessAsync(...)` utilise déjà `IProjectRepository.GetByIdWithMembersReadOnlyAsync(...)`, les queries `GetResourceGroup` / `ListResourceGroupResources` passent par `IResourceGroupRepository.GetByIdReadOnlyAsync(...)`, les handlers read-only `GetKeyVault`, `ListKeyVaults`, `GetRedisCache`, `ListRedisCaches` et `ListStorageAccounts` réutilisent désormais ce lookup no-tracking au lieu du chemin tracké partagé, et `InfraConfigAccessService.VerifyReadAccessAsync(...)` passe maintenant lui aussi par `IInfrastructureConfigRepository.GetByIdReadOnlyAsync(...)` au lieu de `GetByIdAsync(...)`. La tranche courante ajoute en plus le contrat générique `IRepository.GetByIdReadOnlyAsync(...)`, le split `IStorageAccountRepository.GetByIdWithSubResourcesReadOnlyAsync(...)`, et bascule les lectures de détail `GetWebApp`, `GetFunctionApp`, `GetAppServicePlan`, `GetContainerApp`, `GetContainerAppEnvironment`, `GetContainerRegistry`, `GetCosmosDb`, `GetSqlServer`, `GetSqlDatabase`, `GetKeyVault`, `GetRedisCache`, et `GetStorageAccount` sur des lookups read-only. Les flows write/owner projet, les commands `ResourceGroup`, et `InfraConfigAccessService.VerifyWriteAccessAsync(...)` restent volontairement sur les lookups trackés partagés, car ils alimentent ensuite des mutations d'agrégat ou réutilisent l'état de l'aggregate chargé. Le finding reste ouvert pour le reste du sweep read-only. |
| #179 | DB-004 | 🟡 CLOSED-OBSOLETE | Finding d'audit devenu obsolète : `GetConfiguredEnvironmentsByResourceGroupAsync(...)` n'exécute plus 16 requêtes séquentielles. Le repository actuel interroge `vw_ResourceEnvironmentEntries` via `ResourceEnvironmentEntryViews` en une seule requête read-only, et l'interface applicative documente explicitement ce contrat. La consolidation a déjà été introduite par la migration `AddResourceTypeAndEnvSettingsView`; aucune nouvelle refactorisation n'est requise dans la PR #328. |
| #180 | DB-005 | 🟡 CLOSED-OBSOLETE | Finding d'audit devenu obsolète : le modèle relationnel courant expose déjà l'index unique composite `RoleAssignment(SourceResourceId, TargetResourceId, UserAssignedIdentityId, RoleDefinitionId)`. Cette couverture est visible dans le snapshot/migrations actuels et verrouillée par `IndexCoverageConfigurationTests`. Aucune migration supplémentaire n'est requise dans la PR #328. |
| #181 | DB-006 | 🟢 FIXED-IN-PR | `AppSetting.SourceResourceId` ne cascade plus la suppression des app settings quand la ressource source disparaît : la FK passe en `SetNull`, alignée avec `KeyVaultResourceId`, pour conserver l'app setting et invalider seulement la référence. Couverture ajoutée via `DeleteBehaviorConfigurationTests` sur le modèle relationnel EF + migration `SetNullOnAppSettingSourceResource` générée dans la PR #328. |
| #182 | DB-007 | 🔴 OPEN-ACTIONABLE | Duplication dans `StorageAccountRepository`. Extract méthode privée. |
| #183 | DB-008 | 🔴 OPEN-NEEDS-DESIGN | 56 migrations cumulées. Squash demande coordination (perte d'historique vs maintenance). Décision produit. |
| #185 | APP-005 | 🔴 OPEN-LARGE | Remédiation partielle poursuivie dans la PR #328 : l'assemblage de `AppPipelineGenerationRequest` a été extrait dans `IAppPipelineRequestFactory`, supprimant la duplication entre `GeneratePipelineCommandHandler` et `GenerateProjectPipelineCommandHandler`, puis une deuxième tranche a déplacé la récupération des derniers blobs `infra/app/bootstrap` dans `BlobDownloadHelper` (`GetLatestBlobFilesAsync(...)`, `GetLatestDualBucketBlobFilesAsync(...)`) pour simplifier `PushProjectArtifactsToMultiRepoCommandHandler`, une troisième tranche fait désormais réutiliser à `GenerateProjectBicepCommandHandler` et `GenerateProjectPipelineCommandHandler` le `Project` déjà renvoyé par `IProjectAccessService.VerifyWriteAccessAsync(...)` pour appliquer `CanGenerateAllFromProjectLevel()` sans recharger le projet ni les configs domaine, une quatrième tranche introduit `IProjectRepository.GetByIdWithAllAndPipelineVariableGroupsAsync(...)` afin que `GenerateProjectPipelineCommandHandler` et `GenerateProjectBootstrapPipelineCommandHandler` ne chaînent plus deux reloads (`GetByIdWithAllAsync` + `GetByIdWithPipelineVariableGroupsAsync`) du même agrégat projet, une cinquième tranche extrait `MultiScopeGitPushRequestBuilder` pour mutualiser entre `PushProjectGeneratedArtifactsToGitCommandHandler` et `PushProjectArtifactsToMultiRepoCommandHandler` la fusion des scopes Git, le re-slicing des racines, et la détection de collisions de chemins, et la tranche courante supprime enfin le loader privé de blobs de `PushProjectGeneratedArtifactsToGitCommandHandler` au profit de `BlobDownloadHelper.GetLatestBlobFilesAsync(...)`, avec une première couverture directe du handler sur le chargement latest-prefix, la normalisation pipeline, et le not-found pipeline. Le finding reste ouvert pour la décomposition du reste des handlers volumineux (branches profondes de génération/push, mapping détaillé des résultats repo, provider invocation, etc.). |
| #186 | APP-006 | 🟢 FIXED-IN-PR | Sweep local livré dans la PR #328: plus aucun `.First()` ne subsiste dans `InfraFlowSculptor.Application`; les handlers/helpers user-facing utilisent désormais `FirstOrDefault()` + garde explicite, et `AddRoleAssignmentCommandHandler` retombe sur l'agrégat source muté si le repository réhydrate sans `RoleAssignments`. Couverture ajoutée via `AddRoleAssignmentCommandHandlerTests`; `InfraFlowSculptor.Application.Tests` reste vert. |
| #187 | APP-007 | 🔴 OPEN-ACTIONABLE | Double load d'agrégat dans un handler. Identifier le handler concerné et factoriser. |
| #188 | DDD-003 | 🔴 OPEN-ACTIONABLE | Collection mutable exposée sur `CorsRule`. Wrapper `IReadOnlyList<>` + private list. |
| #189 | DDD-004 | 🔴 OPEN-LARGE | Propriétés navigation `= null!`. Sweep important sur tous les agrégats. Pourrait être documenté comme convention EF Core acceptée plutôt que fix. |
| #190 | DDD-005 | 🟢 FIXED-IN-PR | `AzureResource.AddDependency` applique maintenant les invariants minimaux vérifiés par TDD: auto-dépendance interdite, dépendances limitées au même `ResourceGroup`, et création de cycle refusée. Les doublons restent des no-op. Couverture ajoutée dans `ResourceGroupTests`. |
| #192 | GEN-004 | 🔴 OPEN-ACTIONABLE | Injection YAML potentielle. Audit du `PipelineGenerationEngine` pour échappement valeurs YAML. |

### 🟡 MEDIUM severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #193 | DDD-006 | 🔴 OPEN-LARGE | Validation manquante dans constructeurs VOs. Sweep ciblé — `Name`, `AzureResourceName`, etc. |
| #194 | DDD-007 | 🔴 OPEN-NEEDS-DESIGN | Pas de domain events. Demande introduction d'un dispatcher. Décision architecture. |
| #195 | DDD-008 | 🔴 OPEN-NEEDS-DESIGN | Duplication pattern Update. Demande introduction d'une classe abstraite ou d'un mécanisme générique. |
| #196 | DDD-009 | 🟡 PROBABLY-MISGUIDED | `IReadOnlyCollection` vs `IReadOnlyList` — choix stylistique. Si standardisation souhaitée, fix mécanique mais faible valeur. Recommandation : fermer comme not-planned. |
| #197 | DDD-010 | 🔴 OPEN-ACTIONABLE | Allocation inutile dans computed property `CorsRules`. Cache ou yield. |
| #199 | APP-008 | 🔴 OPEN-NEEDS-DESIGN | Masquage incohérent erreurs d'autorisation. Demande politique unifiée 401/403 vs 404. |
| #200 | APP-009 | 🔴 OPEN-LARGE | `CancellationToken` manquant sur `IRepository.GetAllAsync()`. Sweep — toucherait toutes les implémentations. |
| #201 | APP-010 | 🟢 FIXED-IN-PR | `ValidationBehavior` ignore désormais les queries et ne valide que les requests `ICommandBase`, avec tests ciblés. |
| #202 | APP-011 | 🔴 OPEN-NEEDS-DESIGN | Pas de domain services. Décision architecture. |
| #203 | APP-012 | 🔴 OPEN-NEEDS-DESIGN | Repository ne standardise pas l'eager loading. Demande revue de l'interface `IRepository<>`. |
| #204 | API-001 | 🟡 CLOSED-OBSOLETE | Vérification repo-wide sur la branche courante: les `179` blocs d'endpoints `MapGet/MapPost/MapPut/MapDelete/MapPatch` des controllers `InfraFlowSculptor.Api` documentent déjà tous `.ProducesProblem(StatusCodes.Status401Unauthorized)`. Le finding historique n'est plus reproductible et doit être fermé côté GitHub. |
| #205 | API-002 | 🟡 PROBABLY-MISGUIDED | Repo memory : "Response DTO IDs: Always string (not Guid) — Mapster maps Id.Value.ToString()". Le finding pointe l'incohérence comme un bug, alors que c'est une convention documentée. Vérifier les exceptions et soit fixer ces exceptions, soit fermer. |
| #206 | API-003 | 🔴 OPEN-ACTIONABLE | `ErrorOrExtended` ne retourne que la première erreur. Refacto pour aggregate. |
| #207 | API-004 | 🟢 FIXED-IN-PR | `EnumValidation` accepte désormais les valeurs string correspondant à un nom d'enum en case-insensitive; tests étendus. |
| #208 | API-005 | 🟢 FIXED-IN-PR | `TagRequest` utilise désormais des constantes partagées (`512` / `256`) et les collections `Tags` exposées par `SetProjectTagsRequest`, `SetInfraConfigTagsRequest`, `AddProjectEnvironmentRequest`, et `UpdateProjectEnvironmentRequest` sont bornées à `15` via `MaxCollectionCountAttribute`, avec tests contrats dédiés. |
| #209 | GEN-005 | 🔴 OPEN-LARGE | Magic strings API versions Bicep. Centraliser dans une constante par resource type. |
| #210 | GEN-006 | 🔴 OPEN-NEEDS-DESIGN | Gestion d'erreurs incohérente entre engines. Demande standardisation `ErrorOr<>` partout. |
| #211 | GEN-007 | 🔴 OPEN-ACTIONABLE | Identifiers normalization incohérente. Repo memory documente la convention camelCase via `BicepIdentifierHelper`. Sweep des call-sites. |
| #212 | GEN-008 | 🔴 OPEN-ACTIONABLE | Code dupliqué CorsRules parsing. Extract helper. |
| #213 | INFRA-001 | 🟡 PROBABLY-MISGUIDED | `BlobService` Singleton — pattern correct pour SDK Azure stateless avec `BlobServiceClient`. Vérifier mais probablement à fermer comme false-positive. |
| #214 | INFRA-002 | 🟡 PARTIALLY-OBSOLETE | `GitHubGitProviderService` n'utilise plus `new HttpClient()` mais Octokit `GitHubClient` (créé per-call) + `IGitHubTreeApi` Refit injecté. La création de `GitHubClient` per-call reste sub-optimale (pool de connexions), mais pas la fuite originale. À reformuler ou fermer. |
| #243 | [AUDIT][GEN-002] | 🔴 OPEN-LARGE | Manipulation de chaînes non-sécurisée Bicep. Probablement traité partiellement par PR #327 (Refacto bicep generation). À ré-auditer post-merge. |

### 🟢 LOW severity

| # | Code | Verdict | Notes |
|---|------|---------|-------|
| #215 | DDD-012 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #217 | DDD-014 | 🔴 OPEN-LARGE | Raw strings où VOs seraient plus type-safe. Sweep large, faible priorité. |
| #218 | DDD-015 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #219 | API-006 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #220 | API-007 | 🟢 FIXED-IN-PR | (voir ci-dessus) |
| #222 | APP-013 | 🟢 FIXED-IN-PR | La duplication de récupération du dernier lot d'artefacts/blob est désormais centralisée dans `BlobDownloadHelper` via `GetLatestBlobFilesAsync(...)` et `GetLatestDualBucketBlobFilesAsync(...)`; `PushProjectArtifactsToMultiRepoCommandHandler` réutilise ce helper pour les artefacts pipeline et bootstrap au lieu de dupliquer la sélection du dernier bucket horodaté. |
| #223 | APP-014 | 🔴 OPEN-LARGE | Repo memory documente la convention `GetByContained{EntityType}IdAsync` (cf. naming-conventions memory). Sweep des méthodes hors convention. |
| #224 | GEN-009 | 🔴 OPEN-ACTIONABLE | Regex peut matcher dans commentaires/strings. Ajouter parser context-aware ou exclure les zones commentaires. |
| #225 | GEN-010 | 🔴 OPEN-ACTIONABLE | Validation modes de déploiement manquante dans `AppPipelineGenerationEngine`. Ajouter validator. |
| #227 | INFRA-004 | 🔴 OPEN-NEEDS-DESIGN | Pas de `DbSet<AzureResource>` pour requêtes base-type. Demande décision : exposer base-type DbSet vs polymorphic queries via DbContext.Set<>(). |
| #295 | [AUDIT][INFRA-003] | 🔴 OPEN-LARGE | Projections / DTOs non optimisés. Sweep des repositories. |

---

## Recommandations stratégiques pour la suite

### Priorité 1 — Quick wins TDD (PR dédiée 2 jours)
- DDD-010 (#197) : CorsRule allocation cache (1h)

### Priorité 2 — Sécurité (PR dédiée critique)
- GEN-004 (#192) : YAML injection escape

### Priorité 3 — Performance / DB (PR dédiée avec migrations)
- DB-003 (#178) : AsNoTracking sweep

### Priorité 4 — Architecture (décisions à prendre)
- DDD-007 (#194) : domain events dispatcher
- APP-005 (#185) : poursuite de la décomposition des handlers génération/push restants
- APP-001 (#168) : 70 commands sans validator (priorisation)
- DB-008 (#183) : squash migrations

### Probablement à fermer après vérification
- API-001 (#204) : sweep repo-wide validé, `179/179` blocs d'endpoints documentent déjà `401`
- DDD-009 (#196) : choix stylistique `IReadOnlyCollection` vs `IReadOnlyList`
- API-002 (#205) : convention `Id: string` documentée
- INFRA-001 (#213) : Singleton `BlobService` pattern correct
- INFRA-002 (#214) : reformuler ou fermer (Octokit-based)

---

## Suite immédiate

1. Mergeable PR #328 ne contient pas tous les fixes — c'est intentionnel. Le triage ci-dessus produit une feuille de route claire.
2. Proposition : créer 4 PRs dédiées (Quick wins, Sécurité, DB+Perf, Architecture) sur la base de cette priorisation.
3. Issues à fermer immédiatement après validation par l'humain : DDD-009, API-002, INFRA-001, INFRA-002.
